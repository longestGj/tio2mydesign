# Global Header / Footer APP V0.11 Change Impact V0.1

> **CLOSED WITHOUT APPROVAL — 2026-09-01.** This APP-000-only `Quote` / operating-entity proposal never became a shared authority. It is superseded by `GLOBAL_HEADER_FOOTER_SPEC_V0.5.md` and retained only as historical decision evidence. APP-000 V0.12 restores `Logo | RFQ | Menu`, removes the page-specific Footer entity line and consumes the Production SVG shared contract.

## 1. Control

| Field | Value |
|---|---|
| Date | 2026-08-31 |
| Source | User's explicit APP-000 final-polish direction |
| Scope assessed | Shared Mobile Header CTA label and visible Footer operating entity |
| APP proof | APP-000 V0.11 visual package |
| Status | `CLOSED_WITHOUT_APPROVAL / SUPERSEDED_BY_GLOBAL_HEADER_FOOTER_SPEC_V0.5` |
| Does not authorize | Cross-page re-export, development, code, deployment or publication |

## 2. Current shared-baseline conflict

- `GLOBAL_HEADER_FOOTER_SPEC_V0.2.md` remains the last project-control-approved shared baseline and fixes Mobile order as `Logo | RFQ | Menu`.
- `GLOBAL_HEADER_FOOTER_SPEC_V0.3.md` records the user-approved Footer label `Procurement`, but remains `DRAFT_FOR_PROJECT_CONTROL_REVIEW`.
- The current user direction requires a clearer 390px visible CTA label, using `Quote`, and a visible operating-entity sentence in every Footer.

APP-000 V0.11 demonstrates the requested result as a shared-component change candidate:

- Mobile Header: `Logo | Quote | Menu`.
- Mobile Header visible label: `Quote`; intended accessible name: `Request a Quote`.
- Mobile Menu, Desktop Header and Footer CTA: `Request a Quote`.
- Footer buyer heading: `Procurement`.
- Footer sentence: `Operated by IKHLAS TITANIUM (MALAYSIA) SDN. BHD.`
- Existing copyright remains `© 2026 TiO2 Malaysia.`

## 3. Evidence and entity boundary

The exact legal name is confirmed by `00_PROJECT_STATUS.md`, the root project identity and `brand/visual/TiO2_Malaysia_Visual_Standard_V1.0.md`.

The Footer relation is limited to:

`TiO2 Malaysia → website / brand`

`IKHLAS TITANIUM (MALAYSIA) SDN. BHD. → operating entity`

No manufacturer, factory, production-capacity, export-volume, trade, tariff or anti-dumping claim is added.

## 4. Cross-page impact

Header and Footer are shared components. A final shared-baseline change must be applied consistently to Home and page families 03–07. APP-000 alone cannot establish a new global production contract.

Before the V0.11 Chrome may become a global release baseline, project control must:

1. approve a successor shared specification;
2. confirm `Quote` at 390px and `Request a Quote` elsewhere;
3. confirm `Procurement` and the operating-entity sentence across all page families;
4. re-export or otherwise verify all current Header/Footer state boards;
5. verify the Footer sentence is real HTML text and the visible CTA has the accessible name `Request a Quote`;
6. preserve fixed RFQ visibility and the `/request-a-quote/` release blocker.

The proposed change was not approved. `GHF-APP-V11-ENTITY-QUOTE-01=CLOSED_WITHOUT_APPROVAL / SUPERSEDED_BY_GLOBAL_HEADER_FOOTER_SPEC_V0.5`. No instruction in this historical file may override the current shared authority.
