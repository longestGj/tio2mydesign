# 第4步：完整母版隔离验证 V0.1.1

2026-09-11。结论：STEP4_PASS。母版与初始化工具0.1.1具备本地实例安装条件；没有安装D10、启动真实页面任务或验证真实网站生产实现。

## 本轮发现与修正

| 验证对象 | 发现/结果 | 处理 |
|---|---|---|
| integration=null配置 | 原工具抛AttributeError、CLI返回堆栈 | 0.1.1显式校验对象，返回结构化ERROR及退出码2 |
| Windows非法字符 | ?、*、竖线、尖括号未在读取前拒绝 | 0.1.1补充非法字符和控制字符检查 |
| 已有治理保留测试 | 测试写入CRLF却断言LF | 修正测试输入为明确字节；不是工具覆盖问题 |
| 完整包安装 | 15个Agent、9个Skill及依赖逐字节一致 | 通过；安装后副本可独立核验，不回原项目取文件 |
| 重复安装 | 文件内容和mtime保持 | 通过；返回UNCHANGED |
| 既有项目 | 未合并时阻断；测试夹具显式合并后可安装 | 原规则和状态字节保持，不产生竞争Status |
| 缺schema/源文件变化/额外文件 | 包清单校验拒绝 | 目标保持未安装 |
| 过期scope或配置 | 无法复用旧计划 | 拒绝；不能据此声称业务scope已由工具批准 |
| Windows junction | 绑定企图越出项目 | 拒绝，外部目标保持原样 |
| 最终重命名失败 | 注入OS发布失败，实际暂存流程仍运行 | 无半套正式目录，暂存清理后可重试成功 |
| 残缺安装/版本升级 | 不自动修补或覆盖 | 返回阻断，要求单独迁移 |
| Gate9错误站点响应 | 页面和Build相同、scope属于另一站点 | 返回IMPLEMENTATION_FAILURE，未误判PASS |

先运行新增测试，真实发现两类工具缺陷及一处测试输入错误；修正后17项初始化/隔离测试通过。Gate9原8项加错误scope1项通过。最终使用0.1.1完整文件运行合并套件：**26项通过，0失败，0跳过，29.300秒**。

所有网站运行测试只访问临时本地HTTP服务；Git测试仓库由测试在临时目录创建，不操作D16或D10。最终CLI额外在临时空项目执行plan/apply，**未使用--allow-candidate**，成功生成准确版本锁定。证据见[安装冒烟](RELEASE_INSTALL_SMOKE_V0.1.1.json)。

## 发布对象与历史保留

- [母版0.1.1](../../../workflow-packages/industrial-b2b/v0.1.1/README.md)：63项载荷及release-manifest，225条Markdown文件链接闭合；状态INSTALLABLE。
- [工具0.1.1](../../../workflow-tooling/v0.1.1/README.md)：配置schema、示例、受控初始化及26项测试，身份由tooling-manifest维护。
- 0.1.0母版和工具继续保留原候选身份；不覆盖历史批准源、Agent或Skill。
- 母版载荷相较0.1.0仅README和实例绑定的工具可用性说明变化；15个Agent和9个Skill方法正文没有改动，沿用第2步工业泵应用检查，不重复表演一次审查。

INSTALLABLE只表示受控本地安装能力成立，不授予常设关闭、发送消息、开发、部署或发布权限，也不是所有工业品网站的业务适用性证明。

## D10接入预检

对0.1.1重新生成[配置候选](D10_SITE_CONFIG_CANDIDATE_V0.1.1.json)与[准确计划](D10_BOOTSTRAP_PLAN_V0.1.1.json)：67项CREATE均在.workflow，2项MERGE_REQUIRED为AGENTS.md和PLANNING_PROCESS.md。没有写入D10。原main-only/no-worktree、唯一进度和前置策划边界继续有效。

下一步为第5步：按当前文件身份落地具体接入合并，核对已批准前置成果及研究缺口，在授权范围试跑。不得把03研究任务书APPROVED等同研究结论全部完成。

## 明确的验证边界

本次验证了可控的重命名失败，没有模拟操作系统断电、磁盘耗尽或敌对进程在最终检查窗口替换文件。Windows junction已实测，其他操作系统不在本轮验证环境中。HTTP重定向允许列表及真实CMS隔离仍由Gate9适用合同与允许范围控制，未新增机器安全网；错误scope响应负例不是完整生产隔离证明。

Gate3脚本与Gate9脚本没有改写；不声称它们替代语义审查。Gate4/Gate6统一validator仍未实现，继续使用合同允许的等价批量证据。全部锚点语义未自动穷举，文件链接校验不冒充业务正确性。

复核证据：[机器验证记录](ISOLATION_STEP4_VALIDATION_V0.1.1.json)。
