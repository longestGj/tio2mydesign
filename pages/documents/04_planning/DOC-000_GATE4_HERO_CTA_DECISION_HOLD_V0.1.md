# DOC-000 Gate 4 Hero CTA Decision Hold V0.1

## 0. Control

| Field | Value |
|---|---|
| Page | `DOC-000` / `/documents/` |
| Gate | Gate 4 |
| Related Review ID | `DOC-000-G4-DOC-EDITORIAL-01` |
| Decision state | `RESOLVED_BY_DOC-000-G4-HERO-CTA-02` |
| Status | `CLOSED / HISTORICAL_HOLD_RECORD` |
| Date | 2026-09-02 |
| Source | User feedback relayed by project control: current Hero CTA must be replaced; exact final wording not yet confirmed |

## 1. Held item

The current V0.5 key samples render the Hero CTA label:

`Select a Product Grade`

This label is no longer locked and must not be treated as the final Gate 4 candidate. No replacement label is inferred or inserted until project control provides the user's exact confirmed wording and behavior.

## 2. Scope of hold

The hold affects only:

- Hero CTA visible label;
- Hero CTA behavior if the user's final decision changes the approved Gate 3 scroll/focus behavior;
- the Desktop, Tablet and Mobile key samples and any specification line that renders or locks that Hero CTA.

The current raster/SVG assets remain preserved as pre-decision visual evidence. They are not the final Gate 4 candidate.

## 3. Unaffected contracts

- Selector remains immediately after Hero.
- Selector contains one dropdown with the 14 approved Grades.
- Selector's initial `Continue to Request Documents` remains visible, focusable and operable.
- No-selection exact validation and focus return remain unchanged.
- Valid selection Grade-only handoff remains unchanged.
- Four documentation categories, six-question FAQ, closing-action presence and shared Global Chrome remain unchanged.

## 4. Resume condition

Gate 4 final-candidate submission resumed after the user approved `Start a Document Request` as a direct accessible link to `/request-documents/`, with no query, no Selector anchor and no default Grade. The affected specification and three viewport assets must be revised, revalidated and rehashed before project-control review.

Gate 5 remains unauthorized.

## 5. Closure

`USER_COPY_DECISION_PENDING = RESOLVED / CLOSED` on 2026-09-02 by user decision `DOC-000-G4-HERO-CTA-02`. This file remains as the historical hold record; the user-decision and CTA interaction files are the current authority.
