# 通用工作流母版提取对照 V0.1.0

本轮完成实施计划第2步，产物为 EXTRACTED_CANDIDATE，不是可安装发布包。目标新站仍为 gemasterbatch.com / D:\10MasterbatchDev；本轮没有写入目标目录，也没有进行网站制作或开发。

## 修改前后

| 对象 | 原项目做法 | 母版做法 |
|---|---|---|
| 角色与方法 | 角色、方法引用原项目治理和批准基线 | 保留15个角色、9个Skill；运行合同在包内闭合 |
| 企业与产品 | 企业事实、市场、产品关系与项目文件绑定 | 通过实例绑定读取本站来源；不携带原站事实和批准 |
| 共享视觉 | 固定Hero、导航、主CTA及共享消费源 | 由本站批准的品牌、共享owner与组件合同提供 |
| 权限 | 原站已有Gate3/4/6常设关闭授权 | 支持此机制但不授予权限，必须有本站有效授权 |
| Gate3 | 已收紧结构范围 | 保留结构边界、制作上限及预检；不转成视觉制作 |
| Gate4/5 | 同一视觉工作集、唯一独立审查 | 保留工作集和审查合同，不恢复两套制作与截图 |
| Gate6 | 差异审查与开发交付 | 继承有效上游，审本次新增合同，不重做视觉验收 |
| 总控 | 编排、接收、协调和关闭 | 保留接口审计，不成为第二个全量Reviewer |
| 开发与Gate9 | 对接原开发项目 | WordPress＋Next.js适配；目录、scope和Git规则由实例提供；Gate9只读 |
| 证据身份 | 原项目Manifest与基线 | 包文件由release-manifest标识，页面仍由本站唯一Manifest负责 |
| 外部通知 | 文档要求通知相关负责人 | 必须有用户明确的发送授权；否则保存交回内容 |

## 产物与覆盖

母版入口：[README](../../../workflow-packages/industrial-b2b/v0.1.0/README.md)。包内63个载荷文件，加1个身份清单。包括15个角色、9个Skill及其参考、合同、schema、现有脚本和测试，以及实例绑定与技术/行业适配说明。没有新增Gate8专属Agent或恢复Gate7。

来源清单59项，其中56项进入提取，3项外部开发规则仅作为接口参考，不复制成为新站规则。223条原始直接依赖按包内当前合同、实例绑定或历史追溯处理；原批准记录和历史页面不作为新站运行输入。构建脚本为本次提取记录，不是已实现的初始化工具。

1440/768/390、44px和阶段完整覆盖仍为共同质量基线。更改必须有本站决定及相应工具适配，不能只改配置文字降低检查要求。

## 实际验证

- 包内225条Markdown文件链接检查无缺失、无越出包根；未宣称所有锚点语义均经过自动验证。
- 原Gate9工具8项测试通过；迁移后同组8项测试通过，分别用时13.723秒和15.377秒。
- 不同代理只读母版，以工业泵场景逐份应用9个Skill；检查了Gate3越界、Gate5重复保存、Gate6重复视觉审查、Gate9直接修代码、无常设授权和无消息授权六种情境，未观察到违规。
- 复核发现一处V2.1显示标签实际指向V2.2，已同步标签。脚本原本就在Skill内，迁移修正的是调用工作目录说明，并非搬回缺失脚本。
- 方法检查未出现纪律性失败，不虚造red记录；这不是页面制作、真实运行验收或初始化隔离试验。

最终复核时，56项被提取来源均未漂移。两份只读外部参考 development-workflow.md、development-execution.md 已变化，原身份与观察身份保存在验证记录中。未将变化静默纳入母版；接入开发项目时重新核对当时的真实接口。开发根规则未变化。

## 未完成范围与下一步

第3步仍需实现配置、版本锁定与受控初始化，明确CREATE / KEEP / MERGE_REQUIRED / CONFLICT；已有项目不得覆盖原规则或新建竞争状态台账。第4步再进行隔离正反例。两步完成前不得把本包标为可安装版本。

现有工具的能力限制在技术适配中明确：身份/结构检查不等于业务验收；尚未实现统一Gate4/Gate6 validator；真实环境允许列表、重定向和隔离负例还需后续验证。没有访问成功旧站页面、安装D10或验证真实WordPress＋Next.js实现。

逐项追溯：[提取映射](MASTER_EXTRACTION_MAP_V0.1.0.json)；实际检查：[验证记录](MASTER_EXTRACTION_VALIDATION_V0.1.0.json)。D23原角色、Skill与页面批准基线保持原样。
