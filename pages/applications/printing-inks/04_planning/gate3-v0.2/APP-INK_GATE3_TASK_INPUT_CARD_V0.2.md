# APP-INK Gate 3 targeted revision input card V0.2

`DRAFT_FOR_PROJECT_CONTROL_REREVIEW`。范围只限独立审查 `APP-INK-G3-PCR-01` 的 F01–F03。旧 V0.1 冻结、证据和审查报告保留为历史，不覆盖。当前批准页面输入仍是 Manifest V0.3 与 A V0.1 / B V0.2 / C V0.2；正文、事实、URL、主关键词、11 模块顺序和共享合同不变。

- F01：本页 `main` 正文链接消费已批准 CTA 增补的 `#008078`、3px、solid、3px offset 焦点样式。只纠正既有功能合同消费，不增加配色方向或视觉精修。三端以实际键盘 Tab 覆盖 breadcrumb、Hero、Grade、request 与 source 的所有可见正文链接，并验证颜色、可见性和无横向裁切。
- F02：handoff 精确记录 INK-02 为 intro + nine items + one following caution paragraph，INK-05 为 two paragraphs，INK-06 为 four records × three fields + one explanatory paragraph；不拆分或改写 B。
- F03：原共享 owner 验证只继承其实际覆盖，不声称包含 Tab/Shift+Tab 循环。V0.2 页面执行独立跑完 768/390 菜单及三端 Cookie 的正反向循环、Escape、选择退出、背景隔离、焦点/滚动恢复和桌面断点关闭。handoff 为所有后置依赖指定现有 owner、接收阶段和可验证接受条件。

预检通过后建立 V0.2 新冻结并重导三端完整页、两端菜单、390 Grade 锚点和 390 正文焦点状态。最终仍停在独立 Project Control 复审，不自行批准或关闭 Gate 3。
