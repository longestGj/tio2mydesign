# PRODUCT-000 Gate 7 SEO / GEO / Schema / Metadata V0.3

## 0. Control

| Field | Value |
|---|---|
| Page | `PRODUCT-000` / `/products/` |
| Date | `2026-08-31` |
| Review ID | `PRODUCT-000-G7-PCR-04` |
| Status | `PROJECT_CONTROL_REVIEW_PASS / CLOSED` |
| Authority | Gate 6 SEO/GEO/Schema V0.2 plus current V0.3 targeted addendum |

## 1. Search identity

| Field | Exact target |
|---|---|
| Primary keyword | `titanium dioxide pigment` |
| HTML Title | `Titanium Dioxide Pigment Grades | TiO2 Malaysia` |
| H1 | `Titanium Dioxide Pigment Grades for Industrial Applications` |
| Meta Description | `Explore 14 titanium dioxide pigment grades by application, production process and portfolio group, then continue to grade pages for technical evaluation.` |
| Canonical | `https://tio2malaysia.com/products/` |
| Language | `en` |
| Open Graph type | `website` |
| Open Graph title/description/URL | Same exact Title, Meta and Canonical |
| Hreflang | None until a separately approved language URL exists |
| Social image | Omit until separately approved |

Exact-grade, Process, Application, Market, Documents and Resources primary intent remains owned by those pages. Selector states are UI state, not indexable landing pages.

## 2. Route-safe metadata contract

- Title, H1, Canonical and the current Meta are unconditional.
- The current Meta is the approved route-safe Meta from `PRODUCT-000_SEO_GEO_SCHEMA_CONTRACT_V0.2.md` §2 and remains valid when Process or Support route cards are conditionally absent.
- Process and Application classifications remain represented in approved visible page content independently of the conditional route-card modules.
- No live Process route, visible Process card or process-availability condition is a prerequisite for the current Meta.
- Open Graph uses the same exact metadata.
- Process target pages remain external dependencies with independent authorization. The user-authorized PRODUCT-000 Gate 8, after external handoff, consumes their readiness for atomic 2/1/0 route-card integration; Gate 9 verifies resolver output and response/redirect without declaring or implementing the target pages here.
- RFQ visibility never changes metadata. RFQ route readiness remains an independent release blocker.
- Canonical must be emitted server-side and must never use request parameters, a selector state, another site scope or a redirect target outside the approved Malaysia URL.

## 3. Robots and sitemap

| State | Robots | Sitemap behavior |
|---|---|---|
| Development/preview/staging | `noindex, nofollow` | Exclude from production sitemap |
| Gate 8 complete but Gate 9 not passed | `noindex, nofollow` | Exclude |
| Gate 9 passed but Gate 10 not authorized | Keep release-safe noindex state | Exclude |
| Gate 10 user release/index authorization and all blockers closed | `index, follow` | Include exactly canonical `/products/` |

Selector states, query parameters, fragments and conditional module states never enter the sitemap and never emit alternate Canonicals. Child Grade/Process/Application URLs belong to their own page records and are not sitemap entries generated by this page component.

## 4. GEO entity and answer graph

Allowed visible graph:

`TiO2 Malaysia → PRODUCT-000 CollectionPage → titanium dioxide pigment → 14 grade entities → four portfolio groups + six interactive Application result sets + visible Process classification section → individual Grade identities → technical evaluation → conditional procurement paths and RFQ`.

The fourteen exact `directory_summary` values are attributes of the same grade entity only. They do not create hidden relation edges, rankings, guarantees or cross-grade comparisons.

GEO answers must derive from visible content:

- 14-grade count and identities: Hero, directory and FAQ.
- Application result membership: Selector states sourced from PRODUCT V0.3.
- Process classification: the parent section and CR-901 Vapor-phase oxidation row remain visible in all Chloride/Sulfate route-card states; route cards are conditional and must not claim a route live before verification.
- Evaluation method: five-step guide and FAQ.
- Suitability boundary: Hero qualification, Selector disclaimer and FAQ.

## 5. Schema candidate nodes

Schema remains an implementation candidate, not guaranteed search enhancement.

| Type | Required/optional | Contract |
|---|---|---|
| `CollectionPage` | Required | `@id=https://tio2malaysia.com/products/#webpage`; canonical URL, exact name/description, language and site relation |
| `BreadcrumbList` | Required | Home → Products using canonical Malaysia URLs |
| `ItemList` | Required | Exactly fourteen unique items in approved directory order |
| Minimal `Product` | Optional per ItemList item | Same grade identity, URL and exact visible summary only |
| `FAQPage` | Optional | Exactly five visible Q/A; all answers initial DOM; character-identical |

Shared `WebSite`, `Organization` and `Brand` nodes may be referenced only through approved stable IDs emitted by the shared site graph. PRODUCT-000 must not independently invent manufacturer, origin, ownership or certification properties.

## 6. ItemList and Product mapping

### 6.1 ItemList order

M-350, M-510, M-896, M-996, M-2196, M-895, M-200, M-108, M-210, M-340, M-886, M-52, M-2377, CR-901.

Each ListItem uses the approved Grade Page ID/URL from the component mapping. Gate 9 verifies that every URL resolves within `site_scope=tio2-my` before release.

### 6.2 Allowed minimal Product properties

- `@id`, `name`, `url`.
- `description`: exact same `grade_directory[].summary` string used by visible directory; optional, character-identical.
- `category`: only the visible portfolio navigation group, without technical suitability meaning.
- Application `additionalProperty`: optional only if the same positive relation is accessible in the rendered Selector content and comes from PRODUCT V0.3.
- Process `additionalProperty`: omit for Chloride/Sulfate grade membership unless the same grade-to-process mapping is visibly rendered in an approved public surface. Current Process cards alone do not authorize hidden per-grade membership. CR-901 may expose `Vapor-phase oxidation` because the visible row states it.

## 7. Prohibited machine-readable output

- Any relation from the 54 `NO_PUBLIC_MAPPING` rows.
- Negative suitability inferred from non-render.
- Rubber or M-2377 Specialty Materials.
- M-996/M-2196 comparison, equivalence, substitution, superiority, ranking or selection reason.
- `Offer`, `AggregateOffer`, price, currency, availability, inventory, MOQ, lead time or delivery.
- `AggregateRating`, reviews, GTIN, MPN, certification, country of origin, factory, capacity or traceability.
- `ProductGroup`, `isVariantOf`, `isSimilarTo` or equivalent/substitute relations.
- Provisional, unresolved, cross-scope or non-canonical URLs.
- Internal provenance fields or evidence statuses.

## 8. FAQ contract

- All five exact answers are server-rendered in initial DOM even when collapsed.
- FAQ controls may change visibility only.
- `FAQPage`, if emitted, must use the same Q/A strings from the component mapping with no additions.
- Do not add availability, origin, document, performance or comparison claims.

## 9. Metadata and Schema field mapping

| Semantic field | Output | Acceptance |
|---|---|---|
| `seo_title` | `<title>`, OG title | Exact string |
| `meta_description` | meta and OG description | Exact string and condition in Section 2 |
| `canonical_url` | canonical and OG URL | Exact HTTPS URL |
| `language` | HTML language and Schema `inLanguage` | `en` |
| `robots_policy` | environment/release-controlled meta/header | Matches Section 3 |
| `grade_directory[].summary` | Visible text; optional Product `description` | One field, character-identical |
| `faqs[]` | Visible DOM; optional FAQPage | One content source, character-identical |
| `route_registry` | Breadcrumb and item URLs | Malaysia-scope resolved only |

## 10. Gate 9 evidence

Gate 9 must capture rendered HTML and machine output to prove:

1. one H1, exact Title/Meta/Canonical/language;
2. intended robots state and sitemap membership;
3. fourteen ItemList identities, order and URLs;
4. visible summary = Product `description` for every emitted description;
5. no prohibited property or hidden relation;
6. FAQ DOM/Schema parity;
7. no selector URL/canonical/sitemap proliferation;
8. no cross-scope metadata, Schema or route fallback.
