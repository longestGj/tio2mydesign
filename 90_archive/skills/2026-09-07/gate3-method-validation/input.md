# Offline wireframe exercise

This is a synthetic method-validation exercise, not a registered site page, company statement or publication task. All records below are invented. Only write inside the output directory assigned in your task. Do not access D:/16Wordpress_nextjs, use remote services, send requests or change project/Skill files. Use a local HTML planning source and render locally. No paid tools.

Produce one complete responsive wireframe using the exact English copy in copy.json. Its three substantive content types are a technical comparison, a long article and a request form. The supplied content is the fixed exercise baseline; do not edit copy.json. Required logical widths: 1440, 768, 390. Export all complete pages at 1x; also show mobile menu, expanded reading section, invalid submit, and simulated success. Other outputs may be added only where useful. Text is grayscale, use local system fonts; no images required.

This fixture uses its own neutral shared shell solely for testing: visible brand 'Example Evaluation Desk'; desktop navigation Overview, Comparison, Reading, Request, each an in-page anchor, followed by a fixed 'Request review' link. Compact header includes the brand, 'Request review' and a Menu button; menu opens a modal surface containing the same links with Close and Escape support. Focus enters menu, stays inside until closed, then returns to trigger; underlying content is inaccessible while open. Footer repeats brand and one 'Privacy note' in-page link. This shell is expressly not TiO2 Global Chrome and must not be promoted as its proof.

Locked body order: Hero -> Comparison -> Reading -> Request -> Privacy note -> Footer.

Comparison: three example records; all metrics and footnotes must remain unambiguously attached to their record and measurement. Mobile may use cards. These are records to compare, not selectable products. No ranking or recommended winner.

Reading: six sections, full text retained. First section visible initially, remaining sections may use native disclosure with visible headings; preserve the order and provide expanded proof in normal flow. The long text is deliberately meaningful complete prose, not placeholder filler.

Request: required name, required valid email, required desired sample quantity (positive decimal kg), optional record select (one of the three or 'Not sure yet'), optional multiline context. Exact labels and messages in JSON. Name/email/quantity must be labelled; show per-field errors with associations and an error summary; focus the first invalid field. Keep user input on invalid submission. Privacy sentence and its Privacy note link are immediately above Submit, within form. Simulated success is local only, says no message was sent; retains values. No fetching or network submission. Controls at least 44 by 44 logical px where interactive; links can have padded hit boxes. No arbitrary fixed empty space after the form.

Scope: apply the specified Skill to this exercise. Report actual design choices, evidence and limitations. A design-method report is not independent QA or page approval. A checking-method report must not modify candidate files. All interaction claims must distinguish actual local actions from screenshots or simulated receiving behavior.

Runtime available: C:/Users/longe/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node.exe; packages at C:/Users/longe/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules (Playwright available). Local system Python also available. Discover installed Chromium via Playwright or local executables if needed; do not download dependencies.
