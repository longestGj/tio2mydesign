Semrush has country databases rather than a single EU database. Germany (db=de) is used only as a documented EU-market proxy and must not be treated as EU-wide volume. Native CSV export was not used; complete HTML, DOM snapshot, visible text, and table row summaries were saved locally. Metrics are retained exactly as displayed.
