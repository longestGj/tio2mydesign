# 静态站开发规则

先阅读 README.md、CONTRIBUTING.md，检查当前分支、提交和工作区。D23 决定内容和文案；本项目负责实现、验证和发布。

使用 Next.js 静态导出，本地版本化内容；不引入 WordPress、数据库或自建后台。共享组件唯一复用。表单接收采用 Web3Forms，是否包含表单按当前批准批次判断。

新任务从本地 develop 创建 codex/ 功能分支；不直接在 develop/main 开发。独立完成且验证通过的工作项一个 commit，明确暂存本任务文件并检查差异，不夹带他人修改。

源码变更运行适用测试，发布候选在 develop 上集成验证后才快进 main。测试结果绑定具体 SHA；失败不得发布。不覆盖历史证据，不伪造验收结果。

远端仓库为 `longestGj/nextjs_tio2`，仅 `main` 是常规推送目标。经授权推送远端 `main` 后，由 GitHub Actions 对同一 SHA 执行测试、Vercel 生产部署及公开默认域名烟雾检查；三者成功才算发布完成。Vercel Git integration 保持断开，避免双重发布。

远端流水线失败时保留失败回执，不强推、回退或重写 `main`。从本地 `develop` 建修复分支，合回并重新集成验证，再将本地 `main` 快进到新的通过候选并正常推送。自定义域名、DNS、OCI、凭据变更和旧 WordPress 环境操作仍需对应任务授权。

不泄露凭据，不删除其他任务内容。远端 `main` 当前没有平台分支保护，流程约束不能当作平台已自动拦截；启用 Ruleset 需单独配置和验证。

共享知识库只在用户明确要求时查询，不自动读写。迁移历史来源路径只用于追溯，不得成为运行依赖。

Gate 8职责和D23交接见[当前开发交接](docs/gate8-handoff.md)。正式发布的索引条件与预览限制分开；历史noindex批次不成为所有新页面的永久规则。
