const fs = require('fs');
const path = require('path');
const crypto = require('crypto');
const { pathToFileURL } = require('url');
const { chromium } = require('C:/Users/longe/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const root = __dirname;
const candidate = path.join(root, 'product-root-page-hero-preview-v1.0.html');
const output = path.join(root, 'capture-runtime-v1.0.json');
const expectedNav = ['Home', 'Markets', 'Products', 'Applications', 'Documents', 'Resources', 'About'];
const viewports = [
  { name:'desktop', width:1440, height:980, expectedH1:56, screenshot:'PRODUCT-000_ROOT_PAGE_HERO_DESKTOP_1440_CANDIDATE_V1.0.png' },
  { name:'tablet', width:768, height:1200, expectedH1:44, screenshot:'PRODUCT-000_ROOT_PAGE_HERO_TABLET_768_CANDIDATE_V1.0.png' },
  { name:'mobile', width:390, height:1280, expectedH1:36, screenshot:'PRODUCT-000_ROOT_PAGE_HERO_MOBILE_390_CANDIDATE_V1.0.png' }
];
const sha256 = file => crypto.createHash('sha256').update(fs.readFileSync(file)).digest('hex');
(async () => {
  const browser = await chromium.launch({ headless:true }); const results=[];
  try { for (const viewport of viewports) { const page=await browser.newPage({ viewport:{width:viewport.width,height:viewport.height}, deviceScaleFactor:1 }); await page.goto(pathToFileURL(candidate).href,{waitUntil:'load'}); await page.evaluate(()=>document.fonts.ready); const screenshotPath=path.join(root,viewport.screenshot); await page.screenshot({path:screenshotPath,fullPage:false}); const result=await page.evaluate(({expectedH1,expectedNav})=>{ const h1=document.querySelector('h1'),hero=document.querySelector('.hero-shell'),header=document.querySelector('.site-header'),logo=document.querySelector('.brand-logo'),primary=document.querySelector('.button-primary'),selector=document.querySelector('#grade-selector'); const range=document.createRange();range.selectNodeContents(h1); const interactive=[...document.querySelectorAll('a,button')].filter(el=>{const r=el.getBoundingClientRect();return r.width>0&&r.height>0}); const heroStyle=getComputedStyle(hero),pseudo=getComputedStyle(hero,'::before'); const desktopNav=[...document.querySelectorAll('.desktop-nav a')].map(a=>a.textContent.trim()); return {h1FontSize:Number.parseFloat(getComputedStyle(h1).fontSize),h1Weight:getComputedStyle(h1).fontWeight,h1LineCount:new Set([...range.getClientRects()].map(r=>Math.round(r.top))).size,minInteractiveHeight:Math.min(...interactive.map(el=>el.getBoundingClientRect().height)),overflow:document.documentElement.scrollWidth>innerWidth,headerHeight:header.getBoundingClientRect().height,heroBorder:heroStyle.borderTopWidth,heroRadius:heroStyle.borderTopLeftRadius,heroBackground:heroStyle.backgroundColor,heroShadow:heroStyle.boxShadow,pseudoContent:pseudo.content,pseudoDisplay:pseudo.display,currentWordPresent:/CURRENT/.test(document.body.innerText),logoLoaded:logo.complete&&logo.naturalWidth>0,desktopNav,navMatches:JSON.stringify(desktopNav)===JSON.stringify(expectedNav),primaryTarget:primary.getAttribute('href'),primaryTargetResolves:primary.getAttribute('href')==='#grade-selector'&&Boolean(selector),summaryCards:document.querySelectorAll('.summary-card').length,gradeResultCount:document.querySelectorAll('.grade-result').length,h1Matches:Number.parseFloat(getComputedStyle(h1).fontSize)===expectedH1}; },{expectedH1:viewport.expectedH1,expectedNav}); result.name=viewport.name;result.width=viewport.width;result.height=viewport.height;result.screenshot=viewport.screenshot;result.screenshotSha256=sha256(screenshotPath);result.pass=result.h1Matches&&result.h1Weight==='700'&&result.h1LineCount<=3&&result.minInteractiveHeight>=44&&!result.overflow&&result.heroBorder==='0px'&&result.heroRadius==='0px'&&result.heroBackground==='rgba(0, 0, 0, 0)'&&result.heroShadow==='none'&&result.pseudoContent==='none'&&result.pseudoDisplay==='none'&&!result.currentWordPresent&&result.logoLoaded&&result.navMatches&&result.primaryTargetResolves&&result.summaryCards===4&&result.gradeResultCount===8&&result.headerHeight===(viewport.width<=1023?64:84);results.push(result);await page.close();} } finally {await browser.close();}
  const report={candidate:path.basename(candidate),candidateSha256:sha256(candidate),status:results.every(result=>result.pass)?'PASS':'FAIL',viewports:results};fs.writeFileSync(output,JSON.stringify(report,null,2)+'\n');console.log(JSON.stringify(report,null,2));if(report.status!=='PASS')process.exitCode=1;
})().catch(error=>{console.error(error);process.exit(1);});
