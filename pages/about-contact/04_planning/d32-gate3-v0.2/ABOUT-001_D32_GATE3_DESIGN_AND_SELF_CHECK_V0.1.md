# ABOUT-001 D32 Gate 3 — design, input card and author self-check V0.1

Date: 2026-09-21. Author: `/root/about_root_d32`. Stage: `DRAFT_FOR_PROJECT_CONTROL_REVIEW`; author readiness only. This is not an independent review or Gate closure. Reviewer assigned by controller: `/root/market_root_d32`.

## 1. Identity, authority and exact inputs

`ABOUT-001`; `/about/`; EN; `site_scope=tio2-my`; `company_trust`; About owns the company/manufacturer trust intent, not Home's broad supplier acquisition intent, RFQ submission, Contact handling, country customs advice or document delivery. Current public origin is `https://tio2products.com`; this Gate 3 prototype uses relative routes and `noindex,nofollow`. No D32 production runtime is claimed.

Machine input card `input-identities.json` records paths, bytes and SHA-256 of the actual authority sources and shared dependencies. This report plus the machine card is the task input card; the hash in `freeze.json` binds it. The plan's Task 4, Global Constraints and Review Focus govern execution. Root AGENTS, Context, Index, current workflow V3.3, Gate 1–4 standard V2.3, Gate 3 baseline V1.14, its bound execution Agent/Skills and applicable governance §§2,6–13,15–17 are the method/authority combination, not new facts.

Approved content chain: Brief V0.6 and Gate 0–2 approval → Gate 2 content V0.1 → PCR02 user decisions and Gate 5 current visual Manifest V0.3 → Gate 7 content/source map V0.1 with V0.2 media overlay, Gate 7 Manifest V0.2 and Gate 7 project-control closure. Latest visible strings are source map V0.1 §2.1–2.10; media follow V0.2/media handoff. The older Gate 2 no-image direction is superseded by the specific approved media chain. The public company-provided/source/review block was expressly removed; neither a relocated disclosure nor an equivalent summary is rendered. First-party provenance remains in internal mapping and claim boundaries. Missing a general About Playbook is the existing approved page-specific exception, not a reason to invent one.

About RootPageHero binding V1.1 preserves `corporate-light`, approved composite and the 390px natural four-line H1 exception at 36px/700/normal tracking; the V1.0 same-page editable composite is the retained media source. Seven-page shared Hero spec/current manifests establish the component owner and slot boundaries. Open-white remains limited to its approved consumers; this page does not import that surface. D16 Gate 9 closure proves only its historical object, never D32 completion. The old page URL origin, active GA4 state, Next.js code and D16 downstream route blockers do not silently transfer.

Controller's 2026-09-21 ledger rulings select Global Chrome V0.5, neutral Gate 3 consumer V0.2, `no_optional_analytics`, relative routes and the D32 origin. Current D32 Home Manifest V1.12/Gate 8 completion and Home Gate 9 manifest are cross-page reference evidence. Home/Product/APP are family/component references only; no body copy, private rendering code or images were copied from those pages.

## 2. Gate 3 structure and relationship map

| Order / owner | Approved content object | Operation and required relationship |
|---|---|---|
| Shared Header | Production logo; seven root links; About current state; fixed RFQ | Neutral renderer; no public CURRENT badge; mobile menu inherits shared behavior and is tested locally |
| Breadcrumb + RootPageHero slots | Home / About; ABOUT IKHLAS TITANIUM; approved H1 and introductory paragraph; approved decorative composite | Corporate-light shared skeleton projection; Request a Quote → `/request-a-quote/`, Contact Us → `/contact/` |
| Who We Are | Five approved supporting paragraphs; seven company/supply facts | Five approved application phrases → `/applications/`; manufacturer/supplier phrase + Main Product → `/products/`; broad supplier phrase → `/` |
| Why Malaysia Matters | Four exact approved title/body pairs | Manufacturing base/origin/logistics/coordination trust content, no tariff guarantee |
| What We Do | Four exact approved title/body pairs | Supply, grade/application matching, technical documentation and logistics coordination remain bounded service descriptions |
| Markets We Serve | Four approved country/region cards, flags and map | All titles use approved `/markets/` hub fallback; View All Markets → same hub; no new child-route readiness claim |
| Applications We Support | Four approved cards/photos: coatings, plastics, printing inks, paper | `/applications/` fallback and View All Applications; masterbatch exists only in approved inline paragraph, not a new card |
| How We Work | Four sequential steps, semantic ordered list | Product information → application fit → documentation before procurement → reliable communication throughout supply; no page form |
| Documentation & Transparency | Two exact qualified paragraphs; three document action cards | TDS/SDS → `/documents/`; Other Documents → `/request-documents/`; not direct downloadable files |
| Company Facts | Five exact label/value pairs | Operating entity/display facts; does not infer registered office or Schema legalName |
| Final CTA | Exact heading/body; approved decorative background | Same RFQ + Contact owners as Hero; no private submission handler |
| Shared Footer/Cookie | Current approved legal/utility text, no_optional_analytics inactive explanation | Shared Cookie Settings modal; no page-private analytics/consent implementation |

The candidate has live selectable HTML for every heading, paragraph, fact, link and CTA; it is not a full-page raster mockup. `build.cjs` mechanically derives approved §2 strings, shared Chrome, and same-page approved composite into the workset. No new claims or content authoring is hidden in the builder. SVG viewBox projections expose only approved photo/map/flag/background pixels from the approved media canvas; slice containment prevents unrelated raster text leaking into the semantic page.

## 3. Family consistency, justified differences and Gate 4 boundary

Family: seven root navigation pages; shared production logo/Inter/brand colors, fixed RFQ, Header/Footer/legal utilities, root Hero token and responsive contracts. Same-type modules use one title/body/link hierarchy; company facts use a consistent definition-list pattern; ordered process remains ordered at every width. The neutral shared renderer owns Chrome and Cookie; page scripts do not reimplement them. Page-level Hero is explicitly `PROTOTYPE_ONLY` and owns slots, not a production component or competing shared owner.

Necessary differences: About is a long trust/company page, uses corporate-light and its approved images; Markets/Documents/Resources are gateway/list functions. About therefore does not clone Home's hero surface, product cards or imagery. Approved relationship order is fixed in this Gate; the current 4/2/1 diagnostic card distribution is a workable proof, not a new universal column contract. Gate 4 determines final columns, spacing, visual balance, media crop/presentation quality and five-width visual tuning while preserving any already-bound Hero requirements. The 390px four-line H1 is an approved exception, not a defect to solve by shrinking the font or inventing copy.

## 4. Fact/source safeguards and state ownership

| Internal boundary | Preserved interpretation / negative test |
|---|---|
| Manufacturer/company facts | User-approved first-party description, not independent audit or accreditation; keep full IKHLAS TITANIUM (MALAYSIA) SDN. BHD. display name |
| Annual supply / reach / customer scale | Annual supply is not production capacity; factory-customer count is not employeeCount; no independently verified magnitude inferred |
| Origin / location / logistics | Approved Malaysia manufacture/origin and Taiping/Port Klang wording only; no per-batch origin certificate, customs outcome, preferential tariff, export guarantee or registered-office implication |
| Documents | REACH/COA/COO and other support remain grade/destination/request-context qualified; no universal inventory, automatic eligibility or independent certification claim |
| Media | Illustrative approved media do not independently establish ownership of a plant, inventory, office or shipment; no logo-like accreditation or new image requirement |
| Metadata/Schema | Default title/description match approved source map. Gate 3 emits no Schema; Gate 6 must carry visible/metadata/Schema atomic authority and exact D32 origin. No hidden Organization/employeeCount/guarantee added |
| Restricted projection | Local diagnostic only: selectively remove approved-scale facts or affected first-party capability paragraphs/facts, remove unsupported description and neutralize title. Not a new public content state or reversal of user approval |

Full state is the approved default. `?evidence=partial` and `?evidence=restricted` are `LOCAL_SIMULATION` failure-mode evidence, without buyer-facing controls/placeholders. The partial state omits three scale facts; restricted additionally omits affected intro/supporting paragraphs plus location/export facts and related metadata, with the remaining request flow intact. They demonstrate relationship atomicity, not actual CMS toggles or existing D32 content-loss behavior. Real typed editing and fail-closed data rules belong to Gate 6/8. No page-owned loading, submission, search, inventory or download state exists, so none was invented.

## 5. Evidence and author checks

Verifier-first record: `diagnostic/red.json` failed before the editable candidate existed. After initial implementation, diagnostic failures exposed a verifier's glyph-height false positive and asynchronous breakpoint sampling; test logic was corrected without changing shared behavior. `diagnostic/relationship-red.json` then deliberately required the missing five inline application handoffs, contained media crops and atomic restricted metadata; 7 actual failures were repaired. `diagnostic/green.json` and final `diagnostic/final-green.json` show 303 checks passing / 0 failing. No failed record is erased.

Coverage: source-map exact strings and complete H2 order at 1440/768/390; full Header-to-Footer diagnostics; horizontal bounds/no hidden clipped text; all visible targets at least 44×44; full long company name; approved 390 H1 exception; CTA and inline-route ownership; no forms/download attributes, fabricated claims, public governance text or absolute historical origins. Both narrow menus are tested open/first focus/background isolation/Tab/reverse cycle/Escape/focus return/link-selection exit/cross-desktop close. Cookie entry/open/close/focus return and forward/reverse focus cycles are tested in each width. No cross-page route is navigated or real form submitted.

Evidence classes: `ACTUAL_RUNTIME` = local Chromium rendering, measurements, keyboard/actions and capture; `SOURCE_INSPECTION` = source/contract/string and owner matching; `LOCAL_SIMULATION` = hypothetical partial/restricted content states; `INHERITED_REFERENCE` = shared contracts and Home/D16 historical evidence, not this page runtime. Shared interactive checks were expanded to full local revalidation, so no remote/shared runtime pass is substituted. Header/Footer source bytes are neutral-consumer snapshots; full source identity is in input card/dependencies.

Visual author inspection: full diagnostic captures plus 17 readable segments covering every section at three widths were examined. Desktop footer also received a 1440×960 actual viewport check after a tall-viewport diagnostic compositor artifact. Normal runtime footer is complete. Media containment was checked after repair; long facts, body copy, buttons and Footer remain readable at 390. Diagnostic figures are support only, not substitutes for the formal captures.

Evidence-capture revision: the first frozen workset `../d32-gate3-v0.1/` is retained unchanged. Formal capture there failed twice with `Page.captureScreenshot: Unable to capture screenshot` immediately after fonts resolved. A same-source diagnostic waiting two observable animation frames succeeded. Current `d32-gate3-v0.2` changes only capture readiness (fonts ready + two requestAnimationFrame callbacks) and this evidence note; page HTML/CSS/JS and shared bytes are unchanged. No arbitrary sleep is used. Original failure transcript is preserved in `diagnostic/formal-capture-v0.1-failure.json`; first-freeze hashes remain in the old directory. Old diagnostic segments are referenced at `../d32-gate3-v0.1/diagnostic/segments/`; a fresh v0.2 verifier run binds the unchanged page. This is an additive unapproved candidate, not a modification to an approved baseline.

`preflight.json` is the hard `PASS_FOR_FORMAL_RENDER` record; its checker recomputes candidate/dependency bytes. `freeze.json` binds sources, input identity card and preflight before any `formal/` capture. Capture independently rejects changed frozen source/dependency hashes. Formal assets are complete 1440/768/390, menu768/390, partial390, restricted390 and Cookie390. `asset-manifest.json` records dimensions/bytes/SHA-256. `bundle-manifest.json` binds the approval core and retains diagnostic directory/RED evidence pointers separately. Author self-check does not close Gate 3.

## 6. Explicit open items / downstream owners

No known author-stage blocking structural/operation issue remains. Independent reviewer must decide conformance on exact frozen bytes; do not treat this report's PASS claims as independent acceptance. Gate 4 owns five-width visual completion and final module/media polish, including readable media treatment while retaining approved assets. Gate 6 owns typed fields, provenance/claim gating, full SEO/Schema and origin conversion, route availability/fail-closed contracts, migration/recovery and stable acceptance IDs. Gate 8 is exclusively D32 `02开发`; current prototype does not demonstrate those functions.

Markets/Applications hub fallback is an approved routing option, not proof of current D32 destination completion. RFQ, Contact, document hub/request and shared legal destinations are unchanged complete-site dependencies whose runtime readiness is verified later. Any future switch from `no_optional_analytics` to active GA4 requires the shared owner to change legal, consent and implementation atomically; it is not a page-private fix. No merge/push/deploy/release/DNS/indexing/Gate10 action is authorized or taken.
