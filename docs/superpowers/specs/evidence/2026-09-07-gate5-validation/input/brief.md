# Reference Materials Desk — complete-page exercise

This is an isolated synthetic Gate 5 method and receiving-package exercise. It is not a new TiO2 page, product specification, factual company statement or live publication. Exercise ID G5-LAB-01 is a test identifier, not a Page Registry ID. Root has fixed the raw requirements below for this exercise. The real Gate 5 design was user-approved separately; no synthetic page has user Gate 5 approval and no actual Gate 6 is authorized.

## Required outcome and file authority

Produce a complete 1440, 768, 390 logical page and appropriate key-state evidence from the fixed text, layout and visual rules below. Mobile output uses native 2x; Desktop/Tablet 1x. Save an editable offline source, durable relative dependencies, method/final-visual report, preflight/freeze and identity records, actual self-check, and a concise Gate 5→6 receiving package. Describe the synthetic authority and real authorization accurately.

Use D:/23MySec/agents/gate5-execution/agent.md and D:/23MySec/skills/full-page-visual-composition/SKILL.md, with layout-interaction-verification for actual checks. Their candidate header status is superseded for design approval only by D:/23MySec/docs/superpowers/specs/2026-09-07-gate5-design-user-approval-v0.1.md. Never mark this exercise USER_APPROVED or READY_FOR_GATE6_WHEN_AUTHORIZED as if the user approved a page. Supply the handoff as an exercise package with separate completeness/approval/authorization fields.

The fixed text for Orientation, Comparison and Request is the **Exact visible content** section of D:/23MySec/90_archive/skills/2026-09-07/gate4-method-validation/input.md (from Region O through Region R). Its old task instructions and output paths are historical. These three regions retain every text, option, unit, condition, note, media branch and behavior from that fixed-content section. The archived input remains the unique text authority; copying text into render source does not create a new editorial authority.

A usable editable precursor is in D:/23MySec/90_archive/skills/2026-09-07/gate4-method-validation/design-v1/ (index.html, styles.css, app.js, assets/Inter-Variable.ttf). This is a synthetic precursor, not another business page. Source files may be copied into this isolated candidate, with origin and identity recorded. Do not edit the archived precursor or import its old review conclusions. The concrete visual rule table in design-v1/report.md is the exercise's fixed Gate 4 rule input; other report commentary is historical context, not proof of this candidate.

## Complete structure and additional exact text

Order: neutral Header; Orientation O; Comparison C; Reading notes N; Request R; FAQ F; neutral Footer. The precursor supplied O/C/R as representative regions; this exercise completes the shell, N/F and the continuous page. There are no alternate module orders.

N, a plain reading section following comparison, has these exact strings:
- H2: `Keep the context with the result`
- Paragraph: `A number is useful only when its unit, preparation method and observation conditions remain attached. Read the complete record before adding a result to your evaluation notes.`
- Paragraph: `If two records use different methods, preserve that difference in your question. Do not turn a percentage, a concentration and an observed interval into a ranking.`
- Link: `Prepare an evaluation request` → #request.

F, after the request section:
- H2: `Questions about your preview`
- Question 1: `Can I leave the evaluation question blank?`
- Answer 1: `Yes. Choose a reference record to preview it. You can leave the evaluation question blank and add detail later.`
- Question 2: `Will clearing my selection remove the question I typed?`
- Answer 2: `No. Clear selection removes the chosen record and its preview. Your evaluation question stays available.`

Both FAQ items initially closed, each independently toggled by a real button; expose expanded state, visible keyboard focus, full answer in document flow and no clipping. No extra advice, submit promise or factual claim may be added.

## Fixed layout and visual rules

O/C/R keep their raw fixed layouts and the precursor report's values. N uses the existing section container, gutters, H2/body/link roles with paragraphs in one column at every width. F uses the same container/H2 and two stacked disclosures. Use existing white surface, navy title, functional teal border/focus, at least 48px button height and fully wrapping question/answer text. No new palette, font, card treatment, responsive order or media is required. N/F do not invent new direction; they are allowed instances of the supplied rules.

Neutral shell is owned by this exercise input, not TiO2 Global Chrome. Header text: `Reference Materials Desk`. Desktop >=1101 shows `Overview` → #orientation, `Comparison` → #comparison, `Request` → #request. At 768/390 these three links are in a Menu, with toggle label `Menu` / `Close menu`. Header is 84px Desktop and 64px compact, white/navy, with functional teal interactive borders and visible focus; text logo link returns #orientation. Menu opens a white panel below the compact header; first focus goes Overview; Tab/Shift+Tab stays in toggle+links, Escape and link selection close, return focus to toggle after Escape, restore background/scroll, close on Desktop resize. Background main/footer inert while open; fixed height and overflow of body restore on close. No TiO2 logo or company/nav/consent copy. Header/Footer links min 44px both axes. Header menu overlay need not reflow the body.

Footer uses deep navy, white text, existing container/gutters and two wrapping rows, with exact strings `Reference Materials Desk` and `Review the conditions before using a result.`; link `Back to comparison` → #comparison. Footer font >=16px and controls >=44×44; focus white against navy. Use the existing source's body/font/palette rules; no external image, no TiO2 production content. Document height follows content.

## State scope and receiving contracts

At all three widths inspect complete default page, media-absent full composition, long Beta selected, missing-record error with nonempty question, successful Beta preview, FAQ question 2 expanded with keyboard focus. Include compact Menu open at 768 and 390. Full-page default images are required; other states may use relevant regions but must preserve surrounding context and verify resulting page flow. Export only distinct useful proof, no arbitrary image quota. Actual local keyboard, dropdown selection/cancellation, clearing and preserving textarea are required. Full Beta name must be visibly readable in control and preview. Verify the real status at capture time.

Meta title: `Reference Materials Desk — Compare measurement conditions`.
Meta description: `Compare fictional measurement records while preserving units and conditions, then prepare a local evaluation preview.`
Indexing: exercise-only noindex; Canonical absent; JSON-LD absent. This is an offline test, not a production metadata authorization. All material facts derive from the fixed-content input; do not use external web evidence. O illustration is redundant decoration with approved caption, and no-image branch removes figure/caption together.

Receiver: this local preview never sends anything; results mean preview only. A future real receiving service is out of exercise scope. No real request, network fetch or navigation is needed. For Gate 6 handoff list this limitation; do not hide existing Preview actions because a backend is absent. Shared production Chrome/Consent, WordPress/CMS, real robots/Canonical delivery, true browser UI zoom, other engines/devices and assistive-technology output are not verified here. site_scope=tio2-my belongs to any future real project contract; this lab creates no production scoped route or runtime claim.

## Output boundaries

Candidate A formal evidence root: D:/23MySec/docs/superpowers/specs/evidence/2026-09-07-gate5-validation/candidate-a/.
Report: candidate-a/final-visual-report.md. Self-check: candidate-a/self-check.md. Receiving entry: candidate-a/G5-LAB-01_GATE5_TO_GATE6_HANDOFF.md. Candidate manifest: candidate-a/manifest.json or .md, clearly synthetic. Use relative paths and frozen local dependencies so the candidate remains usable. Supporting scripts can reside here as validation evidence; scratch files may use D:/23MySec/99_workspace/gate5-validation-2026-09-07/ but must not be sole presentation dependencies.

Do not write outside the two named output roots; input and archived precursor read-only. Do not edit Agent/Skill, project Index/Status, any real page or D:/16Wordpress_nextjs. Root owns project decision and navigation files. Do not spawn further agents. Save failures and changed candidates distinctly; read back outputs and open all delivered visual assets before return.

Use installed Node/Playwright/Chrome (packages at C:/Users/longe/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules). No installation, paid tools, external website building or publication. Return absolute paths, exact frozen identity, outcome and incomplete items.
