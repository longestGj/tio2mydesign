# DOC-REACH Gate 5 Project-Control Submission V0.1

## 1. Decision requested

Approve `DOC-REACH Gate 5 Complete Visual Baseline V0.1`.

Current status: `PROJECT_CONTROL_REVIEW_PASS_PENDING_USER_APPROVAL`.

Gate 6 is not requested or authorised unless the user explicitly includes that decision.

## 2. Submitted visual

The submission retains the approved `Regulatory Evidence Ledger` direction and completes it with:

- full-page Desktop 1440, Tablet 768 and Mobile 390 logical @2x visuals;
- Mobile Menu open;
- Gate 5 key-state board;
- actual page with the request route unavailable;
- actual legal-actor FAQ expanded with keyboard focus.

## 3. Change from approved Gate 4

Gate 4 established the visual language and representative states. Gate 5 now establishes the default interaction state, captures the important states in the real page and proves the fail-closed route behavior. No approved copy, module order, source set, SEO/GEO direction or visual direction was reopened.

The one functional refinement is limited to the unavailable state: request-specific elements disappear, while the independent `View Document Hub` navigation remains available. This closes `DOC-REACH-G5-R01` and avoids a dead-end experience without creating a Contact fallback.

## 4. Pages and functions affected

- Directly affected: `/documents/reach/`.
- Shared Chrome: consumed unchanged from Global Chrome V0.5.
- Request receiver: `/request-documents/` eligibility/Schema relationship is specified but its UI is not changed here.
- Related routes: `/documents/` and `/markets/european-union/` remain ordinary destinations.
- DOC-TDS and DOC-COO remain unchanged.

## 5. Validation

- Gate 4 retained direction/structure contract: PASS.
- Gate 5 complete visual/state contract: PASS.
- 7/7 raster assets generated and hashed.
- Superdesign server/local HTML parity: exact match.
- Automated and original-scale manual review: PASS.
- P0/P1/P2=`0/0/0`.
- `DOC-REACH-G5-R01=VERIFIED_FIXED / CLOSED`.
- Independent review's Desktop 44px finding and three minor evidence findings were test-first corrected; `DOC-REACH-G5-R02=VERIFIED_FIXED / CLOSED` and final Critical/Important/Minor=`0/0/0`.

## 6. Remaining limitations

- This is a planning/design artifact, not implemented WordPress/Next.js output.
- `ECHA_TIO2_DIRECT_ANSWER_PUBLICATION_BLOCKER=OPEN`; the TiO2-specific registration answer remains absent.
- Official source dates and route/form/Schema atomicity require later implementation and Gate 9 verification.
- Sensitive documentation remains request-controlled; no public download behavior is specified.
- Gate 6–10, development, D16 changes, deployment, publication, DNS and indexing remain unauthorised.

## 7. Project-control recommendation

`DOC-REACH-G5-PCR-01 = PROJECT_CONTROL_REVIEW_PASS_PENDING_USER_APPROVAL`

Recommendation: `APPROVE GATE 5`.

The submission covers every required viewport and applicable important state, preserves the approved content/evidence boundaries, keeps ordinary Hub navigation available in the fail-closed state and contains no unresolved P0/P1/P2 findings.

## 8. Expected user decision

- `批准 Gate 5。`
- `批准 Gate 5，并授权启动 Gate 6。`
- specific revision instructions.
