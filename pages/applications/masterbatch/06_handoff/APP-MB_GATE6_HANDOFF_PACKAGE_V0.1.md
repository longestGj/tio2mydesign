# APP-MB Gate 6 Development Handoff Package V0.1

Date: 2026-09-08. Package ID: `APP-MB-G6-HANDOFF-01`. Dispatch: `G6-APP5-20260908-01/MB`. Executor: `/root/gate6_app_masterbatch`. Execution result: `READY_FOR_REVIEW`. This is the single Gate 6 development-delivery entry prepared for independent review. It does not approve or close Gate 6, perform an external handoff, authorize Gate 8, or authorize implementation, deployment, publication, DNS, sitemap or indexing.

## 1. Intake, authority and route

| Intake item | Exact accepted object and conclusion |
|---|---|
| Approved bundle | A `APP-MB_GATE2_CONTENT_SKELETON_V0.2.md` SHA `633a2165…44dc`; B `APP-MB_GATE2_FULL_BUYER_CLEAN_COPY_V0.3.md` SHA `06bdd245…343f`; C `APP-MB_GATE2_CONTENT_CONTRACT_V0.3.md` SHA `944fee91…168`. B remains the sole editable visible-copy source. |
| Structure and complete visual | Gate 3 approved structure is inherited through Manifest V0.8. Gate 4 freeze `APP-MB-G4-FREEZE-01`: editable source `APP-MB_GATE4_EDITABLE_SOURCE_V0.1.html`, 34,676 bytes, SHA `96ef54b2…9c1e`; freeze record SHA `fab491dd…599`. |
| Effective independent review | `APP-MB-G5-INITIAL-01`, reviewer `/root/g5_masterbatch`, author `/root/gate4_masterbatch_execute`, `REVIEW_PASS`, required Findings 0; report SHA `8a7d45d8…ea4c`. |
| Controller closure | `APP-MB-G5-PC-VISUAL-CLOSURE-01`, `APPROVED / CLOSED` under `G346-DELEGATED-CLOSURE-20260907`; accepted visual input is the exact combination in that record, SHA `29bb95f3…11d2`. |
| Gate 6 authorization | User instruction to use subagents to complete Gate 6 for the five named Application pages; Controller dispatch `G6-APP5-20260908-01/MB`. Write scope is this page's `05_review/gate6-v0.1/` and `06_handoff/` only. |
| Original open dependencies | `APP-MB-O02`, `APP-MB-O03`, freeze `unverified[0..4]`, and `SHARED-CONSENT-001`, retained in §7 without renaming their source identities. |
| Changes after closure | Current Manifest `APP-MB_CURRENT_GATE_BASELINE_MANIFEST_V0.9.md`, 3,248 bytes, SHA `546f514a…195a`, still points to the same accepted freeze/review/closure. No content, machine-semantics, frozen source, key dependency or Finding change was identified before this package. |

Path: `FAST_PATH`. The accepted content, structure and frozen visual identities remain valid; the independent visual review is complete and there is no contrary evidence or open required Finding. This Gate 6 execution therefore inherits that full visual conclusion and checks the new cross-contract development mapping, receiver boundaries, shared ownership, machine meaning, dependencies and Gate 9 conditions. It does not rerender, export screenshots or rerun the Gate 5 interaction suite.

## 2. Development source and reuse boundary

Gate 8 starts from the current site implementation and shared capabilities, then evaluates reuse or adaptation of the frozen HTML. The frozen source is a complete visual reference and directly opens with local production-logo SVG references; it is not asserted to be a production component or directly mergeable. Its CSS and interaction script are embedded. Its only file dependencies are the approved primary and reverse logo SVGs referenced through the production logo manifest. The approved evidence set comprises full-page 1440/768/390 images, 768/390 menu-open images and a 390 Cookie-open image listed in the freeze record.

The production page consumes the shared Header, Footer, menu, fixed RFQ, Logo, legal utilities and consent implementation owned by their existing owners. A local copy in the prototype is visual evidence, not authority to create a page-specific production fork. There is no approved body image and no embedded APP-MB form. Gate 8 may choose WordPress models, API fields, React/Next components and cache mechanisms only after inspecting the current repository; those names are deliberately not invented here.

## 3. WordPress-managed semantics and Next.js observable result

| Scope | WordPress must preserve/manage | Next.js must observably render |
|---|---|---|
| Identity | `APP-MB`, language `en`, page type Application, navigation key `applications`, `site_scope=tio2-my`; registered route remains provisional | One English Application page at the route approved by the route owner; Applications active in shared navigation, with no buyer-visible governance label |
| `MB-01` | Exact H1, direct answer and two in-page action labels/targets from B/C | Hero with `Build Your Evaluation Basis` → `#define-the-two-stage-system` and `Review Masterbatch Grades` → `#grades-to-review`; neither creates qualification state |
| `MB-02`–`MB-03` | Two-stage system inputs and comparison-basis copy, including method/equipment/conditions/test-polymer and no universal threshold | Extractable semantic headings and complete text; qualifiers remain attached to the relevant comparison claims |
| `MB-04`, `MB-06`, `MB-07` | Four-row evidence-role matrix, four-row document-role matrix and four-step buyer-owned evaluation sequence as structured records | Semantic tables at wide widths and fully labelled records at 390px; row/object/question/value/limit/decision relationships remain intact |
| `MB-05`, `MB-08` | Processing/final-use boundary, APP-PLAS handoff and equal-accepted-endpoint economics limits | No cross-polymer/service-life inference, saving, winner, ROI or commercial result; APP-PLAS link only when its owner marks the route eligible |
| `MB-09` | Exactly seven neutral relations in approved order: M-510, M-200, M-108, M-210, M-340, M-886, M-2377; corresponding five Chloride/two Sulfate labels | Seven equal cards/rows with self-selected links; no rank, recommendation, fit, equivalence, qualification, performance or availability. An ineligible Grade relation is omitted atomically from visible and machine output |
| `MB-10` | Request-preparation copy and three distinct action relations owned by CONV-DOC, CONV-SAMPLE and CONV-RFQ | Separate links to `/request-documents/`, `/request-sample/` and `/request-a-quote/`; no embedded form and no claim that click/submission means delivery, supply, quote, sample or approval |
| `MB-11` | Four source records with entity, exact linked title, source type and approved date/no-date note | Four readable source notes with their links and scope; private comments and internal evidence/Gate status never render |

Empty or ineligible data must fail closed within this page contract: do not use another scope, fabricate placeholder copy, show empty cards, publish disabled dead links, add a negative suitability statement, or substitute a different Grade. Missing required local content is an implementation/release error. Conditional omission must keep visible links, metadata and Schema synchronized.

## 4. Actions, receiver and context boundary

APP-MB supplies only the approved page context. Each Conversion owner controls its form fields, validation, receiver, privacy notice and success/failure semantics.

| Action | Context APP-MB may supply when the current receiver contract permits it | Required production behavior |
|---|---|---|
| Documents | One known Grade, one or more requested document types, and editable `application=masterbatch`; another Grade requires a separate request | Human review determines availability and applicable scope; no direct download or availability promise |
| Sample | Known Grade or unknown-Grade choice, masterbatch application, destination and test objective | Submission starts review; any arrangement is confirmed separately |
| RFQ | Known lead Grade or `Not sure / Need help`, masterbatch application, required or estimated quantity in metric tonnes, destination and known context; other Grade names belong in `Additional Requirements` | Only a verified receiver response may trigger the approved received/success state; the team then reviews details and contacts the buyer using supplied information |

Gate 8 must use the currently approved receiver contracts and allowlists. It must not infer query parameter names from this package, trust public `site_scope` or receiver values, make context immutable, or convert page context into a recommendation. If an eligible receiver or safe context mapping is unavailable, the affected action fails its dependency and cannot be represented as working.

## 5. SEO, GEO, Schema and social delivery

- H1: `Titanium Dioxide for Masterbatch`.
- SEO title: `Titanium Dioxide for Masterbatch Evaluation | TiO2 Malaysia`.
- Meta description: `Evaluate titanium dioxide for masterbatch by separating concentrate processing from final-article evidence. Review Product Grades and prepare your request.`
- GEO direct answer: `Evaluate a TiO2 pigment candidate in two connected stages: making the concentrate, then letting it down into the receiving resin and testing the final specimen.`
- Canonical candidate: `https://tio2malaysia.com/applications/titanium-dioxide-for-masterbatch/`; it must not become final through Canonical, Schema, robots, sitemap or social URL before `APP-MB-O02` closes.
- Baseline Schema: `WebPage` and `BreadcrumbList`. `ItemList` is allowed only when the same seven eligible Grade links and order are visibly rendered. `Product`, `Offer`, `Article`, `TechArticle`, `FAQPage`, `QAPage`, rating, recommendation, equivalence, performance, qualification, availability, price and delivery-result relations are prohibited.
- Open Graph/Twitter title, description, URL and any structured summary must preserve the same approved meaning. A social image may be used only if the shared/site owner supplies an approved asset; no unapproved factory, inventory, capacity, certification or final-system result image is introduced.
- SSR/initial HTML must expose the H1, direct answer, substantive modules, links and applicable metadata/Schema without client-only loading. Machine output must disappear atomically with an ineligible visible relation.

## 6. Shared consumption and seven-surface `site_scope` isolation

Use `GLOBAL_HEADER_FOOTER_SPEC_V0.5.md` (SHA `cc236f47…9f18`), CTA addendum V1.0 (SHA `1f6cb1f9…7e9a`), Production SVG Logo Manifest V1.0 (SHA `81b73a52…894`), Global Footer Legal Utility Addendum V1.0 (SHA `c1f00114…633`), No-Terms decision V1.0 (SHA `9b01c3d0…27d1`) and shared consent baseline V1.1 (SHA `be03ef11…659`) through their owner implementations. Header uses the approved primary logo, Footer the reverse logo; fixed RFQ remains shared. Footer exposes Privacy EN/BM, Cookie Policy and Cookie Settings, with no Terms link. Consent remains aligned to the approved no-optional-analytics state unless a later approved owner record changes it.

Every production surface below must be bound to `site_scope=tio2-my`, fail closed when the local object is missing, and never read, render or fall back to another business/site scope:

1. **Query:** WordPress content, relationships, eligibility and settings are queried with trusted server-side scope.
2. **Route:** the approved route resolves only the APP-MB record in `tio2-my`; wrong/missing scope cannot resolve another site's page.
3. **Cache:** keys, tags, revalidation and invalidation include scope so another site's page or metadata cannot populate APP-MB.
4. **Menu:** navigation data, current-state calculation, fixed RFQ and Footer utilities come from the `tio2-my` shared owner set.
5. **SEO:** Canonical, robots, sitemap participation, hreflang decision, social metadata and Schema are generated from the same scoped page/route eligibility.
6. **Form:** Conversion targets, allowlists, prefill/context and receiver selection use trusted `tio2-my` configuration; browser-supplied scope/receiver values are never authoritative.
7. **Media:** logo and any future approved media resolve through `tio2-my` or explicitly shared approved assets; missing media cannot fall back across scope.

## 7. Open dependencies, acceptance and failure disposition

| ID | Requirement and current evidence | Owner / validation stage | Acceptance condition | Failure disposition |
|---|---|---|---|---|
| `APP-MB-G6-D01` | Final route is unresolved (`APP-MB-O02`; `PROVISIONAL_URL`) | Architecture/Controller; before public Gate 9/10 | One approved route decision synchronizes route, Canonical, sitemap, robots and Schema/social URL | Blocks final routing/indexing/publication; do not finalize the candidate URL implicitly |
| `APP-MB-G6-D02` | APP-PLAS handoff eligibility from `APP-MB-O03` | APP-PLAS owner; Gate 8/9 | Correct live scoped route and contextual handoff work; visible and machine relations agree | Omit atomically until eligible; blocks that relationship and release if the complete-site contract requires it |
| `APP-MB-G6-D03` | Seven PRODUCT V0.3 Grade relations/routes | Product/Grade owners; Gate 8/9 | Each rendered route resolves to the correct `tio2-my` Grade; list/order/process labels and ItemList parity hold | Omit only the ineligible relation atomically; no substitute/dead card; unresolved required relations block release |
| `APP-MB-G6-D04` | CONV-DOC, CONV-SAMPLE and CONV-RFQ actions, receiver/prefill and actual receipt from `APP-MB-O03` | Conversion and receiver owners; Gate 8 implements, Gate 9 verifies | All three eligible routes preserve context/allowlists and actual receiver outcome semantics; positive receipt evidence exists where success is claimed | Affected action fails; no Contact fallback, fake success or delivery promise; unresolved required receiver blocks release |
| `APP-MB-G6-D05` | Shared Chrome, Logo, legal utilities and `SHARED-CONSENT-001` | Shared/Home, Legal/Privacy, consent and deployment owners; Gate 8/9 | Approved shared components render and operate with correct links, keyboard/focus behavior and current consent inventory | Blocks shared integration acceptance and release; no page-local production fork |
| `APP-MB-G6-D06` | ISO and manufacturer source freshness; last approved checks include ISO confirmation 2025 and manufacturer access 7 Sep 2026 | Content/source owner; immediately before release | Current authoritative pages still support the bounded visible predicates, or approved copy is revised through its owner | Blocks affected time-sensitive predicate/publication; does not silently reopen unrelated approved facts |
| `APP-MB-G6-D07` | Actual browser zoom, physical devices, other browsers and assistive technology were not performed on production | Gate 9 QA | Production implementation preserves complete copy, semantic tables/records, 200% reflow, keyboard/focus and assistive semantics across the required environments | Blocks Gate 9 accessibility/responsive acceptance and release until evidenced |
| `APP-MB-G6-D08` | Production CMS/SSR/cache/Schema/scope/receiver/consent implementation is not present in this D23 task | Gate 8 implementation owner; Gate 9 read-only QA | Development receipt binds every §8 AC to commit/ref, worktree state, environment, implementation/data source and actual evidence | No Gate 9 or release PASS; package/self-check/local prototype cannot substitute |

## 8. Stable Gate 9 acceptance conditions

Gate 8 returns the same IDs with actual commit/ref, worktree status, environment, implementation/data source and evidence. Gate 9 tests the rendered implementation; a package assertion, screenshot alone, HTTP 2xx or local simulation is insufficient where behavior or receiver semantics are required.

| ID | Observable acceptance / required evidence | Failure and owner |
|---|---|---|
| `APP-MB-G9-AC01` | Approved scoped route resolves one English APP-MB page; Page ID/type/navigation state are correct and no governance text renders. Evidence: route response plus rendered DOM/visual. | Wrong identity/scope or visible internal state fails; Gate 8. |
| `APP-MB-G9-AC02` | All 11 modules render in C order with B Buyer Clean copy and four sources; normalized text/heading/source-link comparison against B/C. | Missing/reordered/expanded meaning fails; Gate 8/content owner. |
| `APP-MB-G9-AC03` | Two-stage, comparison, evidence, document and evaluation relationships remain semantically attached; wide tables and 390 labelled records preserve every row/field. Evidence: DOM semantics and 1440/768/390 inspection. | Detached qualifiers, unlabeled mobile values or lost rows fail; Gate 8. |
| `APP-MB-G9-AC04` | Exactly seven eligible Grades appear in approved order with five Chloride/two Sulfate labels and equal treatment; no prohibited inference. ItemList, if present, exactly matches visible eligibility/order. | Rank/substitution/dead relation or machine-only relation fails; Product owner/Gate 8. |
| `APP-MB-G9-AC05` | Hero anchors reach their exact sections; APP-PLAS and Explore Products links resolve when eligible without changing ownership. Evidence: keyboard and pointer activation plus target/URL result. | Broken target or implicit selection/qualification fails; Gate 8/route owners. |
| `APP-MB-G9-AC06` | Documents, Sample and RFQ are distinct eligible actions with the §4 contexts and owner semantics; actual receiver tests prove claimed success/receipt and failure recovery. | Cross-wired receiver, immutable/unsafe context, fake success or delivery promise fails; Conversion/receiver owners. |
| `APP-MB-G9-AC07` | 1440/768/390 complete page, menus and Cookie state match the accepted visual hierarchy; no clipping, text clamp, horizontal page overflow or broken Footer. Evidence: actual production views and geometry. | Visual/reflow defect fails; Gate 8/shared owners. |
| `APP-MB-G9-AC08` | Keyboard order, visible focus, menu/Cookie focus trap and return, Escape, reduced motion, semantic headings/tables/labels and >=44×44 targets satisfy approved behavior; production 200% zoom and assistive checks included. | Any required inaccessible state fails; Gate 8/shared owners. |
| `APP-MB-G9-AC09` | WordPress can manage the approved semantic module content, source records, Grade/action relationships, eligibility and metadata within `tio2-my`; empty/ineligible values fail closed without fake content. Evidence: scoped CMS/API records and edits reflected in render. | Hard-coded competing copy, cross-scope data or placeholder output fails; Gate 8. |
| `APP-MB-G9-AC10` | Next.js SSR/initial HTML exposes H1, direct answer, modules, links and applicable metadata; cache/revalidation changes remain scoped. Evidence: initial HTML, cache-key/tag and update/invalidation test. | Client-only substantive content or cache bleed fails; Gate 8. |
| `APP-MB-G9-AC11` | SEO title/meta exactly match §5; Canonical/robots/sitemap/social URL follow the final route decision; no hreflang is added without a new contract. Evidence: rendered head and sitemap/robots output. | Premature/forked URL or stronger social claim fails; SEO/Gate 8 owner. |
| `APP-MB-G9-AC12` | WebPage/BreadcrumbList and conditional ItemList validate and state only visible approved meaning; all prohibited Schema types/relations are absent. Evidence: rendered JSON-LD plus visible parity review. | Hidden, stronger or stale machine relation fails; SEO/Gate 8 owner. |
| `APP-MB-G9-AC13` | Shared Header/Footer/menu/fixed RFQ/Logo/legal/Cookie consume current owners; Applications is active, no `CURRENT` or Terms renders, approved primary/reverse logos load. Evidence: DOM, visual and interaction checks. | Local fork, wrong utility/link/logo or stale shared behavior fails; shared/Gate 8 owners. |
| `APP-MB-G9-AC14` | Query, route, cache, menu, SEO, form and media pass correct/wrong/missing-scope tests with no cross-scope fallback; public scope/receiver tampering is ignored/rejected. | Any data/route/cache/menu/metadata/receiver/media leakage is a release blocker; Gate 8/security owner. |

## 9. Cross-contract review, Findings and untested boundary

The source-to-package and package-to-source checks found no content, fact, action, machine-semantics or owner conflict. New implementation requirements are traceable to approved B/C, accepted Gate 4/5 material, shared contracts or the project-wide scope rule. Stable required Gate 6 Findings: **0**.

Untested here: production WordPress/API records, Next.js SSR and route behavior, caching, metadata/Schema output, actual Conversion receivers, production consent inventory, physical devices, browser zoom, other browsers, assistive technology, deployment, DNS, sitemap/indexing and current external-source freshness at release. These are assigned to §7 dependencies and §8 acceptance IDs; none is represented as passed.

Status: `READY_FOR_REVIEW`. A different identity must independently review this exact package. Only Project Control may then close Gate 6 and update the current Manifest. Closure would mean `APPROVED_FOR_HANDOFF / HANDED_OFF=NO` until a separate actual handoff and Gate 8 authorization are recorded.
