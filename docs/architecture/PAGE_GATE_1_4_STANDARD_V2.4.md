# TiO2 Malaysia 页面 Gate 1–4 统一标准 V2.4

> 2026-09-07当前批准方式：Gate 3、4、6按[总控常设关闭授权](GATE_3_4_6_DELEGATED_CLOSURE_AUTHORITY_V1.0.md)执行独立审查与总控关闭，不再等待逐页用户批准；Gate 1/2/9/10不变。本文历史“用户批准”表述只保留原记录语境，当前三关关闭由该授权解释；内容事实决定、明确暂停、下一阶段启动与外发/开发/发布权限分别保留。


> 2026-09-07：V2.1仅同步Gate 3最终控制条款，保留[V2.0](PAGE_GATE_1_4_STANDARD_V2.0.md)的Gate 4/5合并结果。继承[Gate 1–5标准V1.3](PAGE_GATE_1_5_STANDARD_V1.3.md)的Gate 3定向修订，并按[Gate工作流V3.4](GATE_WORKFLOW_V3.4.md)将原Gate 4视觉方向与原Gate 5完整视觉合并为当前Gate 4；4A为内部检查点，4B为唯一正式关闭点。旧标准保留。

2026-09-21按[Gate 3/4合并决定](GATE3_GATE4_MERGE_DECISION_V1.0.md)执行，历史限定授权不扩大。

## 1. 文档控制

| 字段 | 当前值 |
|---|---|
| Document ID | `PAGE-GATE-1-4-STANDARD` |
| Version | `V2.4` |
| Status | `APPROVED_PROJECT_STANDARD / ACTIVE` |
| Approval source | G34-MERGE-20260921：用户批准Gate 3/4合并设计及执行；继承此前有效标准 |
| Effective date | `2026-09-21` |
| Scope | `D:\23MySec` 中所有页面的 Gate 1–4 策划、内容、视觉和审批 |
| Governing authority | 根 `AGENTS.md`、当前批准 PRD、页面登记册和页面专属 Brief |

本文件是 Gate 1–4 的唯一详细流程标准。页面 Playbook 和页面专属 Brief 可以增加更严格要求，但不得降低本文件的交付、审批、版本和证据要求。

## 2. 核心原则

1. 所有页面使用同一 Gate 1–4 结构；页面内容可以不同，交付格式、批准规则和当前基线管理不得各自解释。
2. 用户可以用自然语言、截图、草图、中文或英文提出页面想法。页面执行任务负责把输入规范化为 Brief、内容骨架和正式合同，不要求用户先写技术文档。
3. 页面必须尽早让用户审查“要说什么”，再进入“怎么排”和“长什么样”。不得等完整视觉稿完成后才第一次暴露内容方向。
4. 提交不等于批准；项目总控复审通过不等于用户批准；某一 Gate 获批不自动授权下一 Gate。
5. 已批准旧页面不因本标准生效而自动重做。下一次修订或进入下一阶段时补齐当前 Manifest、权威引用和缺失的响应式证据。
6. Gate 3–4 暂不调用付费 Superdesign 插件。页面执行任务继续使用项目既有的本地规格、HTML/CSS/脚本渲染、PNG 导出和人工复核流程；工具选择不得降低任何交付、响应式、内容真实性、视觉或审批标准。除非用户以后明确重新授权，否则不得因插件默认触发规则、执行任务偏好或追求视觉变体而调用 Superdesign 或产生相关费用。

## 3. 用户想法的最早输入点

### 3.1 Gate 0 / Gate 1：Page Intent Card

每个页面的 Brief 必须包含一张用户可直接审查的 Page Intent Card：

| 必填项 | 说明 |
|---|---|
| Audience | 主要访问者是谁 |
| Buyer problem | 访问者需要解决什么问题 |
| Core message | 页面最重要的一句话 |
| Must include | 用户要求必须出现的内容、事实、图片或表达 |
| Must exclude | 明确不出现的内容、承诺、页面关系或视觉元素 |
| Primary CTA | 页面最主要的下一步 |
| Secondary CTA | 可选的第二行动；没有时明确写 `NONE` |
| Ownership boundary | 本页负责什么、哪些内容归其他页面 |

Gate 1 结束前必须形成检查点 `CONTENT_INTENT_CONFIRMED`。未确认时可以继续研究，但不得把内容方向写成已批准方案。

### 3.2 Gate 2：内容骨架先审，完整文案后审

在进入完整设计前，先提交一页可快速判断的内容骨架：

- H1；
- Hero 核心信息；
- 模块顺序；
- 每个模块的一句话目的；
- 主 CTA、次 CTA 及目标 URL；
- SEO Title、Meta Description 和 Canonical 方向；
- GEO / Schema 拟表达的实体与关系；
- 受限事实、条件模块和隐藏规则。

内容骨架确认后，再用真实完整文案完成 Gate 2。进入 Gate 4 前必须形成检查点 `FULL_COPY_AND_MODULE_ORDER_CONFIRMED`。这两个检查点属于现有 Gate，不新增 Gate 编号。

## 4. Gate 1–4 标准交付

### 4.1 Gate 1：研究、关键词与证据

**目标：**确认页面身份、搜索意图、买家问题和可公开事实边界。

**强制交付：**

- 已填写的页面 Brief 与 Page Intent Card；
- 主关键词、辅助关键词、排除关键词和防内耗边界；
- 搜索意图、目标采购阶段、买家问题和内部页面关系；
- 事实与证据表，至少包含来源、来源日期、适用范围和验证状态；
- GEO 实体、可能的可引用答案和时间敏感事实清单；
- 禁止公开、尚未验证、条件公开和明确冻结的字段；
- `CONTENT_INTENT_CONFIRMED` 记录。

**Gate 1 锁定：**页面身份、关键词所有权、事实边界和研究范围。证据缺失只冻结受影响字段，不得编造替代事实。

### 4.2 Gate 2：完整内容、SEO 与 GEO 架构

**目标：**在视觉布局前确认页面实际要发布的内容和机器可读表达。

**强制交付：**

- 已确认的内容骨架；
- H1、Hero、全部模块标题和真实完整正文；
- 模块顺序及每个模块帮助买家完成的判断；
- CTA 文案、URL、预填字段、上下游内链和面包屑；
- SEO Title、Meta Description、Canonical、索引和语言方向；
- GEO 的实体、关系、简明答案、FAQ、来源和日期；
- Schema 类型、字段来源和可见文案一致性规则；
- 正常、空值、证据受限、route 未就绪和表单失败等条件状态；
- `FULL_COPY_AND_MODULE_ORDER_CONFIRMED` 记录。

**Gate 2 锁定：**真实文案、模块顺序、链接关系、CTA、SEO/GEO/Schema 事实范围。Gate 4 不得用短占位文字替换真实内容。

### 4.3 Gate 3：历史结构阶段

新页面的跨页一致性与结构验证并入Gate 4，不要求单独完整线框、正式冻结、独立结构审查和关闭。历史Gate 3成果/授权按[合并决定](GATE3_GATE4_MERGE_DECISION_V1.0.md)继承；仅获旧Gate 3授权的任务不得自动开展完整视觉。

### 4.4 Gate 4：页面设计与验证

**目标：**在一个端到端阶段中确认视觉方向，并用真实文案、完整三端页面和适用状态证明可供独立审查、总控依授权关闭与Gate 6接收。

**4A内部检查点 `VISUAL_DIRECTION_CHECKED`：

- 接收Gate 2批准全文/顺序/行为、品牌及共享合同，明确页面家族、参照页面批准身份、同类组件、本页应用、必要差异和owner缺口；不强索新Gate 3冻结。
- 确认内容/操作关系与真实状态；复杂表格、表单或新家族按风险局部验证，成熟模板直接继承。不默认另交三端正式线框或独立结构审查。**

- 在批准品牌内确定色彩、字体、间距、卡片、按钮、表单、图片、图表、媒体和状态规则；
- 确定Desktop、Tablet、Mobile的列数、尺寸、间距及响应式布局，复验完整内容与操作关系；
- 用真实高风险区域和必要状态证明方向能够应用；
- 在4A建立跨页映射并继承有效旧Gate 3成果，核对Global Chrome、生产Logo及相关同类页面；统一组件外观与操作含义，内容页不强套首页；
- 记录可继承规则、需要整页展开的实例和未决冲突。

4A不产生Gate批准或`APPROVED / CLOSED`。方向足以裁决且没有超授权变化时，执行Agent可以连续进入完整页面制作；重大品牌、共享、内容或结构变化按责任边界提交决定。

**4B完整视觉与冻结：**

- `1440px` Desktop、`768px` Tablet、`390px` Mobile完整页面；Mobile允许`780px @2x`导出；
- Header至Footer的全部批准内容、模块连接与共享组装；
- Mobile Menu open及页面适用的重要交互、表单、错误、空值、受限、无图片和长内容状态；
- 最终视觉规则、同一设计源及耐久依赖、三端与状态证据、操作证据、冻结身份；
- 一个当前权威Manifest入口，指向自检、最终独立审查、Finding/复审和批准记录。

完整视觉必须检查真实内容、首屏、按钮后空白、横向溢出、裁切、Footer闭合、最小44px宽和高的触控目标、字号、对比度及键盘/焦点。三张完整页面图是基础证据；长页需可读分段，适用交互需实际操作证据。静态、运行、源码和模拟结果分别记录，策划模拟不代替Gate 9开发验收。

冻结后只设一个Gate 4最终独立审查环节；该环节可以包含Finding、返修和多次定向复审。独立审查通过且必修关闭后，由总控依常设授权确认准确4B冻结组合，Gate 4才`APPROVED / CLOSED`。

**Gate 4锁定：**总控依常设授权关闭的视觉语言、完整视觉、文案呈现、响应式表现、适用交互状态及冻结组合。Gate 4批准不授权Gate 6总审与交付、Gate 8开发、部署、发布或索引。

## 5. Global Chrome 统一所有权

Header、Mobile Header、Mobile Menu 和 Footer 是全站共享组件，由 Home / Global Chrome owner 维护唯一生产基线。页面任务只负责：

- 声明本页的 current navigation key；
- 在完整页面中展示组装后的共享组件；
- 验证本页正文与共享组件的连接、响应式和 Footer 闭合；
- 引用当前 Global Chrome 规范、Manifest 和资产键。

页面任务不得自行重设计、复制分叉或改变 Header/Footer，不得改变 Logo、一级导航顺序或固定 RFQ 规则。可见导航不得显示内部状态词；当前页只使用视觉 active treatment，不显示 `CURRENT` 等内部标签。

## 6. 状态与批准

需要用户决定的 Gate 成果统一使用以下状态链：

`DRAFT_FOR_PROJECT_CONTROL_REVIEW`

→ Gate 1/2：`PROJECT_CONTROL_REVIEW_PASS_PENDING_USER_APPROVAL`；Gate 4经Gate 5审查：`PROJECT_CONTROL_REVIEW_PASS`

→ `APPROVED / CLOSED`

规则：

1. 页面任务只能提交草案，不能自行写成项目总控 PASS 或用户批准。
2. Gate 1/2沿用原用户批准流程；Gate 4经Gate 5独立审查PASS且必修关闭后，总控依常设授权直接关闭，不等待用户。
3. 当前Manifest记录批准依据、关闭对象、范围、审查者、关闭人与日期；合并Gate 4记录USER_STANDING_AUTHORITY及授权ID，不伪造逐页用户审阅。
4. Gate 1–4 的检查点和局部决定可以在 Brief 中记录，但不得伪装成整个 Gate 已批准。
5. 任一 Gate 的 `APPROVED / CLOSED` 只关闭该 Gate，不自动打开下一 Gate。

## 7. 当前唯一 Manifest

每个页面必须维护一个当前权威文件，推荐命名：

`PAGE-ID_CURRENT_GATE_BASELINE_MANIFEST_Vx.x.md`

Manifest 至少包含：

- Page ID、URL、页面类型和生命周期；
- Gate 1–4 当前权威文件；
- 当前视觉资产、逻辑/物理尺寸、版本、字节数和 SHA-256；
- Gate 4 `workset_id`、共同输入索引、设计源、耐久依赖和唯一证据索引；
- 4A `VISUAL_DIRECTION_CHECKED`记录、4B冻结身份、最终独立审查及Finding/复审指针；
- 静态图、实际本地运行、源码观察和本地模拟的证据分类与未验证范围；
- 当前 Global Chrome 版本、Manifest 和 asset keys；
- 批准对象、日期和来源；合并Gate 4记录常设授权ID、独立审查、关闭人与日期，逐页用户审核NOT_REQUIRED；
- 被替代版本及保留路径；
- 开放问题、事实冻结、外部依赖和下一 Gate 授权状态；
- 明确的 authority / consumption order。

同一页面不得同时存在两个未标明优先级的“current”完整 Brief、视觉规格或 Manifest。历史文件必须保留并标记 `SUPERSEDED`、`HISTORICAL` 或等效状态，不得删除或反向控制当前版本。

## 8. 批准后的变更

已批准内容发生实质变化时：

1. 复制当前版本并升级版本号；
2. 登记变更原因、影响范围、回退基线和新 Review ID；
3. 只重开受影响的 Gate 或定向审查范围；
4. 生成新 Manifest，并将旧基线标记为被替代但可回退；
5. 涉及文案事实的变化同步检查 SEO、GEO 和 Schema；
6. 涉及布局的变化重新检查 1440、768、390；
7. 未获新批准前，旧批准基线继续是对外与开发权威。

## 9. 既有页面过渡规则

- 不要求已经批准的页面立即重做 Gate 1–4。
- 页面下次修订或进入下一阶段时，必须建立/更新一个当前 Manifest，并明确历史版本优先级。
- 既有页面缺少 768px 证据时，应在下一次视觉修订补齐；在补齐前必须登记为明确的 carry-forward item，不能把 1440 或 390 直接当作 Tablet 证据。
- 既有页面的 Header/Footer 视觉可以继续作为组装证明，但生产实现和开发合同必须引用共享 Global Chrome，不得要求每页独立开发。
- 过渡整理不得改变已经批准的页面内容、事实、视觉或阶段授权。

## 10. Gate 4最终提交前最小检查表

- [ ] Page Intent Card 已确认。
- [ ] 完整真实文案与模块顺序已确认。
- [ ] 关键词、事实、SEO、GEO、Schema 和 CTA 边界一致。
- [ ] 1440、768、390 完整证据齐全。
- [ ] Mobile Menu 和适用状态齐全。
- [ ] 一个共同工作集、设计源和证据索引可定位，4A检查与4B冻结状态分开。
- [ ] 适用交互有实际操作证据，静态、运行、源码和模拟结果没有混写。
- [ ] Global Chrome 只引用共享基线，无页面级分叉。
- [ ] 无内部治理词进入 Buyer Clean。
- [ ] 当前 Manifest 唯一且包含尺寸、版本和 SHA-256。
- [ ] 历史版本保留并明确 superseded 状态。
- [ ] 总控依授权关闭范围与下一Gate启动权限分别记录，未伪造逐页用户批准。

## 11. 版本记录

| 日期 | 版本 | 决定 |
|---|---|---|
| 2026-09-07 | V2.1 | Gate 3定向修订最终整合；保留V2.0全部合并视觉条款。 |
| 2026-09-07 | V2.0 | 原Gate 4/5合并为当前Gate 4；4A内部检查，4B唯一正式关闭；一个执行Agent、三个能力Skill和一个最终独立审查环节。 |
| 2026-09-07 | V1.3 | 用户授权Gate 3定向收敛：结构停止条件、共享证据继承、状态图按风险、视觉建议归属和结构/参数交接。 |
| 2026-09-07 | V1.2 | Gate 6统一总审与开发交付；Gate 7历史兼容，8/9/10编号与授权边界保持。 |
| 2026-09-05 | V1.1 | 用户决定暂不使用付费 Superdesign 插件；Gate 3–5 继续采用既有本地设计与验证流程，交付和审批标准不变。 |
| 2026-09-01 | V1.0 | 用户批准建立统一 Gate 1–5 标准、两个早期内容确认点、1440/768/390 视觉证据、共享 Global Chrome 所有权和单一当前 Manifest 规则。 |
