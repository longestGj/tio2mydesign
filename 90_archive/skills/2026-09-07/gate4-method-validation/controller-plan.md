# Controller-only evaluation plan

Date: 2026-09-07. User: “执行下一步”, following the proposal for an isolated real-length content exercise and independent examination. Authorization is for method behavior validation, not a real page or runtime registration. No approval of new factual content is inferred.

## Design

One synthetic task with Orientation/Comparison/Request regions, fixed text and layouts; Inter copied read-only from existing project asset. Designer receives raw task and candidate Skill, but no injected-defect list or expected review outcome. Designer output is not assumed defect-free. No no-Skill control arm is planned; this cannot establish causal improvement or general accuracy.

An independent reviewer will examine actual design output against raw requirements. A separately scoped reviewer may examine a controlled derivative. Both receive original requirements and source/assets, not designer self-check, controller results or this plan before initial findings. Agent hierarchy may be visible; do not describe the experiment as strictly blinded.

## Planned controlled derivative (not yet built)

Once the first source is available, preserve it unchanged and create a separately versioned derivative with a small number of recorded deviations:

- functional CTA colour reverted to the older decorative teal on a light surface;
- a visible certification-like claim absent from the allowed content;
- a narrow-screen content/relationship or selected-value visibility defect.

Exact mutations, expected acceptance failures and source hashes must be written before the derivative is dispatched. Actual rendered mutated exports must correspond to the mutated source; stale-export failure is not automatically tested by this plan.

## Evidence and limits

Controller will independently inspect returned rules/report/assets, verify actual bytes/hashes/dimensions and sample screenshots, and perform targeted local interactions where feasible. Designer self-check, independent review, controller checks and injected defects are distinct records. Findings require source location, observed behavior, buyer impact and acceptance criteria. Preserve failures, revised sources and re-review records.

Only demonstrated method ambiguities justify narrow Skill edits; ordinary execution mistakes should first be repaired in the artifact. A corrected output does not prove all browsers, all page types, production chrome, real submission, assistive technology, write failures or the full Agent workflow.

At completion, map every exercise file and copy all unique evidence into a permanent archive under D:/23MySec/90_archive/skills/2026-09-07/gate4-method-validation/. Verify path count and SHA-256 before/after copying; preserve working originals. Save a concise project validation record referencing archived reports, input, source snapshots and results, and update only the Gate 4 navigation/source status as warranted.
