# APP-PAPER Gate 4 Workset and Visual Rules V0.1

Date: 2026-09-07. Author: `/root/gate4_paper_execute`. Workset: `APP-PAPER-G4-WORKSET-V0.1`.

This is the common workset for brand-applied-visual-design V0.4, full-page-visual-composition V0.4 and layout-interaction-verification V0.8 SELF_CHECK. All consume `APP-PAPER_GATE4_INPUT_INDEX_V0.1.json`, one `APP-PAPER_GATE4_COMPLETE_VISUAL_V0.1.html` and one final `APP-PAPER_GATE4_EVIDENCE_INDEX_V0.1.json`. This is an execution record, not a page current Manifest or approval.

Authorization is the Controller dispatch to this actual author under `G4-REMAINING-8-EXEC-01`. Stop: `GATE4_EXECUTION_COMPLETE_FROZEN / READY_FOR_GATE5_REVIEW`. Only this Gate 4 work directory, execution self-check and Gate 4→5 handoff may be written. Gate 5 review, closure, Gate 6, development and publication are excluded. No subagent was used.

The dispatch referenced method baseline V1.8. The current Index selects V1.9, and the Controller explicitly confirmed its use. V1.9 changes the downstream review/acceptance reference only; the Gate 4 V1.4 role, three skills and common contract V1.2 are byte-identical to the V1.8 declared identities. No scope expansion follows.

## 4A — VISUAL_DIRECTION_CHECKED

Buyer task: define the actual paper system, distinguish method and evidence roles, review two neutral Grades, and prepare an owner-specific request. Use the approved Industrial/Technical Editorial brand direction without media. Actual source uses Inter, Navy headings, white/Soft surfaces and functional Teal. No factory, performance graphic, status badge or inferred product claim is introduced.

| Role | Actual rule | Application |
|---|---|---|
| Hero | Solid `#062B5B`; white H1 60/48/40px; first paragraph 19/18/18px; scope paragraph 17/16/16px | PAPER-01; complete approved text and two anchors |
| Section hierarchy | H2 38/34/30px; content padding 68/56/46px vertically; existing 1120px inner width | PAPER-02–11; natural content height |
| Body | Inter 17px desktop, 16px narrow, line-height 1.68; paragraphs up to900px | All reading blocks |
| Technical tables | 16px, line-height1.62; Navy/white header; body object labels600; existing column proportions | PAPER-03/04 |
| Responsive records | Existing two columns at768, one at390; labels14px; white cards with12px corners | PAPER-03/04/09; each complete field retained |
| Grade neutrality | Same row/card surface, type, border and link weight for M-350 then M-2377 | PAPER-09; no default choice or ranking |
| Actions | `#008078` functional color; primary white text; secondary outlined; hover `#00645E`;7px inherited corners | Hero and owner request links; min44×44 |
| Focus | 3px white on Hero;3px Teal on light page surfaces; shared focus unchanged | Keyboard states |
| Sources | Seven ordered records,15px/1.7, light separators, complete source links and status language | PAPER-11; no source scope changes |
| Shared components | Exact page-neutral owner renderer payload; original SVGs and Inter file | Header/Footer/Menu/Cookie, navigation Applications |

Representative diagnostic samples are regions of the same provisional whole-page source, not alternate layouts. Viewed at readable scale: 1440 READ_PANEL1–4 (Hero, tables, identity/laboratory/evidence/cost chain, Grade table, three request cards, sources and Footer);768 READ_PANEL1–2 (two-column technical/Grade records and single request sequence);390 READ_PANEL1–2 (all fields, long source names, equal Grade records and full-width actions). Each multi-column reading panel is a lossless tiling of consecutive vertical slices, read top-to-bottom then left-to-right; it is not a proposed layout. Narrow Menu and all Cookie diagnostic states were opened and inspected separately.

Agent judgment: direction serves the approved paper evaluation and preserves the full content and structural relationships. Representative risks are resolved within visual parameters. `VISUAL_DIRECTION_CHECKED`; no new brand, structure, fact or owner decision is required. The provisional full-page application is accepted into4B in the same workset; its valid initial runtime and image coverage is reused, not repeated as a second method run.

## 4B composition and single SELF_CHECK

The source is derived from this page's approved Gate3 source, preserving the exact main markup and owner payload, and appending only page-specific visual rules. The approved B remains the sole editable copy authority. PAPER-01–11, all15 body links, all seven sources and every technical record are retained. No A-only eyebrow/breadcrumb was reinstated; the approved B/Gate3 treatment takes precedence.

`APP-PAPER_GATE4_INITIAL_OBSERVATIONS_V0.1.json` records90 executed assertions.86 are valid PASS. Four measurement-expectation failures are retained: three font scans counted the deliberate zero-size Hero separator container, and the no-JS test expected14 links instead of the actual approved15. `APP-PAPER_GATE4_TARGETED_OBSERVATIONS_V0.1.json` replaces these four expectations with actual corrected measurements and adds overlay target geometry and actual Hero hover/focus checks:15/15 PASS. No HTML, CSS, assets or business behavior changed for this correction. Effective coverage is101 distinct assertions;105 assertions were executed including the superseded four. These counts describe this run only.

Actual runtime coverage includes normalized complete B text and link parity at all three widths;11 modules;4 optical and4 evidence records;2 equal Grade rows;7 sources; typography, asset load, geometry, clipping and44px targets; Menu entry/forward/back loop/Escape/selection/background isolation/breakpoint closure; Cookie entry/forward/back loop/background blocking/Escape/Close/focus restoration; both Hero anchors; no-JS content and effective720px reflow proxy for200% desktop zoom. The latter is viewport reflow, not a real-browser zoom or assistive technology claim.

Visual coverage is the complete initial three-width reading panels plus the two Menu and three Cookie states. No clipping, collision, abnormal fixed whitespace, unequal Grade treatment or failed Footer closure was observed. Source links use full multiline targets; natural punctuation wrapping is retained without shortening the approved source identity. The page has no form, filter, FAQ, optional-analytics banner, media or success/error UI; such states are not applicable.

Color checks calculate nine actual page pairings, including normal/hover CTA, body on Soft, white on Navy and label text; ratios are stored in initial observations. Shared source and exact owner asset payload are verified independently by string/identity comparisons, while the interactions above are actual current-page operations; no previous shared report is claimed as new local loop evidence.

After preflight, the source and durable dependencies are locked before formal capture. The formal run captures only full-page/state evidence and does not rerun the content/geometry/interaction suite. Capture check and PNG/source/dependency identity audit precede the final freeze. Formal files, source and final reports form approval_core; initial images, lossless readable panels and detailed runs remain diagnostic_support. No copied bitmap is represented as native2x; every image is native DPR1.

## Open items and boundaries

No required Gate4 visual Finding remains. Production routes and URL approval, receiver behavior, canonical/index eligibility, CMS/Schema, scope isolation in implementation, real-device/browser zoom/assistive-technology behavior, analytics and release remain later owner responsibilities. Target URLs are the approved full-site planning destinations; this local file does not prove they are live. No external link request or form submission was sent. The registered URL remains `PROVISIONAL_URL`; no canonical or production Schema was added.

Gate6 should retain the architecture/destination owners' route eligibility requirement, CONV-DOC/CONV-SAMPLE/CONV-RFQ receiver and field contracts, shared Chrome/Consent owner behavior and `site_scope=tio2-my` isolation. Gate8 implements; Gate9 verifies the actual result; Gate10 remains separately authorized. Gate4 readiness is not independent review, closure, handoff to development or publication.
