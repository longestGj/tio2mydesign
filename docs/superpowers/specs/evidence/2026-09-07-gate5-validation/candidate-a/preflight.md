# Candidate A preflight — synthetic exercise

2026-09-07. Execution self-check, not independent review or page approval. Result: `PASS_FOR_FORMAL_RENDER` for the fixed G5-LAB-01 scope after the corrections below.

Inputs were read directly: exercise brief, archived exact O/C/R content and concrete rule table, archived editable precursor, approved Agent/Skill design and authorization record, full-page composition method and layout/interaction verification method. The precursor is a synthetic exercise; no business page was used as a template. Source dependencies are four relative, durable files under `source/`.

Actual run: `diagnostic/preflight-06/runtime.json`, generated by `verify-render.cjs preflight-06`. Chrome 152.0.7977.82 / Playwright 1.62.1 / Node 24.16.0; 1440×1000 at 1x, 768×1000 at 1x, 390×844 at native 2x. All three complete default and absent pages, real selection/error/preview, FAQ2 focus and compact menus were captured; source hashes are recorded in the runtime.

All tested states had viewport-matching scroll width, no measured out-of-viewport or hidden overflowing containers, controls at least 44×44, loaded Inter, and minimum HTML text size 16px. Fixed O/C/R strings were checked from raw input in both source and actual document. N/F and neutral shell exact text were compared directly with the exercise brief and viewed in the complete page. Metadata and local anchor targets match the exercise. No HTTP(S) requests or page errors occurred.

Visual examination: complete default pages opened and then examined at readable scale through overlapping crops at all three widths. O illustration/caption and media-absent O→C transition inspected at every width. Dense tablet table retains all values, units, conditions and notes. Mobile cards preserve their associations. Mobile Beta result and FAQ2 focus/answer/footer viewed. Subsequent formal self-check opens every formal state asset. The neutral shell and N/F apply the supplied existing roles, without production Chrome claims.

Correction history (preserved, no old files deleted):

- `SELF-01` — actual source behavior defect, compact menu Escape: returning focus to the toggle could undo the restored scroll position. `diagnostic/preflight-02/runtime.json` records failure. Prior script source is `diagnostic/app-before-scroll-fix.js`. Current source returns focus using `preventScroll:true`. Preflight 03 reached the following independent assertion, and preflight 05/06 verified 500px scroll restoration at both compact widths, Escape focus return, cleared body styles, background inert removal, link-close and Desktop-resize-close. This is an author correction and self-retest, pending independent review.
- `TOOL-01` — capture error in preflight-01, not a page defect: a document-coordinate clip was requested with viewport-only capture. Enabled full-page coordinate capture for section evidence. Failure JSON and initial image retained.
- `TOOL-02` — assertion error in preflight-03/04, not a page defect: required missing style attribute (`null`) rather than accepting an empty restored attribute (`""`). Actual close had `#comparison`, no inert background and no active body styles. Assertion now checks cleared style content; raw diagnostic confirms the distinction. No source change made for this issue.
- Capture refinement after preflight-05: request evidence now includes the complete following FAQ heading; FAQ evidence begins with complete preceding action buttons. Preflight-06 confirmed revised boundaries without changing page source.

Not verified: production receiving service, shared production Chrome/Consent, CMS/WordPress, server robots/Canonical delivery, browser UI zoom, other engines/devices or assistive-technology output. These are explicit lab exclusions, not unresolved required local state failures. Preview is an offline local simulation of a prepared request and sends nothing.
