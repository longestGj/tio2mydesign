# RES-000 D32 Gate 3 structure V0.1

Date: 2026-09-21. Author: `/root/res_root_d32`. State: `DRAFT_FOR_PROJECT_CONTROL_REVIEW`. This is a D23 planning consumer, not D32 code or a Gate approval.

## Authority and identity

Page remains `RES-000 /resources/`, EN, `site_scope=tio2-my`, `NO_PRIMARY_KEYWORD`. The task's approved implementation plan, Task 3, authorizes this additive Gate 3 candidate only. Approved content is `RES-000_CONTENT_ARCHITECTURE_V0.3.md` (B); SEO/GEO/Schema is `RES-000_SEO_GEO_SCHEMA_CONTRACT_V0.2.md` (C), as identified by `RES-000_GATE5_APPROVED_BASELINE_MANIFEST_V0.1.md`. Brief/Playbook provide unchanged eligibility and evidence controls; B supersedes their earlier module/control suggestions. `RES-000_GATE7_MANIFEST_V0.1.md` and the D16 Gate 9 closure prove historical objects, not a D32 implementation or present child eligibility.

The frozen input and dependency inventory is `d32-gate3-v0.1/RES-000_D32_INPUTS.json`. Shared Hero authority is the V1.0 shared spec + open-white addendum + seven-page approved baseline; RES consumes its existing `hub-dark` contained variant and `RES-000_ROOT_PAGE_HERO_GATE4_BINDING_V1.2.md`. Open-white exceptions for Home/APP do not apply to RES. No private Hero variant is proposed.

The controller's 2026-09-21 clarification inherits the user's 2026-09-20 D32 origin decision: new D32 canonical is `https://tio2products.com/resources/`; old B/C/overlay remain immutable. This Gate 3 uses relative routes and a local-only noindex preview. Gate 6 must record the D32 canonical/og:url/schema origin mapping explicitly. The planning predicate defaults to the new origin, not the historical one.

## Complete content/operation structure

| Order | Module | Invariant / actual H0 behavior |
|---|---|---|
| 0 | Global Header | Unmodified page-neutral Chrome V0.2 consumer; Resources `aria-current=page`, fixed RFQ, production SVG |
| 1 | Breadcrumb | Home → Resources; Home is an ordinary relative site link |
| 2 | RootPageHero | Exact B eyebrow, H1, supporting copy, one primary local action; `hub-dark`; decorative research-path diagram only |
| 3 | Featured, conditional | Actual qualified inventory=0: root, heading, card, action, divider and spacing absent |
| 4 | Three research paths | Exact three B labels/titles/paragraphs; guidance articles, not fake navigation cards |
| 5 | Latest, conditional | Actual qualified inventory=0: whole root absent; no placeholder or duplicated Featured item |
| 6 | Evidence standards | Exact three B principles; compact supporting safeguards, no new claims |
| 7 | Buyer questions | Exact five approved procurement questions and full answers; all visible without JavaScript |
| 8 | Global Footer | Unmodified neutral consumer; shared legal utility links and Cookie Settings; no local anchor variants |

Hero action goes to `#research-paths` in H0/H1 and to `#featured-resources` only when Featured actually renders. No page form, selector, search, filter, process comparison, grade recommendation, merchant offer, sample request or child-page creation is introduced. Shared RFQ remains owned by shared Chrome; no form was submitted.

The editable build script extracts B's existing strings into the workset. Derived copy JSON is a traceable generated projection, not a second copy authority. H0 HTML is the only actual-current preview. H2 and H4 HTML files are separately bannered `LOCAL SIMULATION`, excluded from real inventory and prohibited from production seed/API/HTML/Schema/cache. H2 illustrates the exact approved RES-ORIGIN design candidate; it does not upgrade it. H4 includes explicitly synthetic long title/Portuguese agency/source/scope/date values, uses example.invalid links, and asserts no real policy outcome.

## Inventory, freshness and atomic behavior

Current qualified public count, Featured count, Latest count and ItemList count are all zero. Historical candidate/route/child Gate 9 records cannot enter inventory automatically. B/C approval or a registered route alone is insufficient. Predicate order: scope+language → distinct Page ID → mapping+URL+content+claim+public eligibility+live route → title/summary/canonical → valid review dates and current review interval → type-specific audit → selection/order. Unknown types, cross-scope content and canonical mismatch fail closed. `RES-000_D32_RESOURCE_MODEL.cjs` is executable planning notation, not a production eligibility implementation or real verification of supplied booleans.

Guide review maximum is 180 days, technical 90, active Trade 30, stable Trade 90; earlier due dates and event-triggered review hold exclude immediately. Technical comparison also requires technical and non-equivalence audit. Trade requires explicit approved CURRENT state, source verification, Market, Topic, official source name/HTTPS link, source date, scope, last review, public status wording, fact/analysis label, freshness owner and next due date. This preview makes no current regulatory assertion and therefore does not treat fixture URLs as research evidence.

H1 public output=H0. H2 puts the single qualified child in Featured, never again in Latest. H3 allows at most three Featured and nonduplicative latest entries ordered by real publication/review metadata. H4 presents source, scope, dates, status and action together. H5 recomputes all public arrays so card/metadata/action/ItemList are removed together; future D32 must extend that single eligible-array rule through server HTML, API, Schema, menu/query/cache paths. No hidden JSON-LD inventory exists in H0. Schema implementation itself belongs to Gate 6/8; C's CollectionPage and BreadcrumbList semantics remain required.

## Cross-page family and responsive relationships

| Component | Owner / reuse | Necessary RES difference |
|---|---|---|
| Header/Footer/Menu/Logo | Home/Global Chrome + neutral Gate 3 package V0.2 | Only current Page ID and absolute production Logo asset slots supplied |
| Cookie Settings | Current D32 shared Cookie/Consent owner | RES neither owns nor activates analytics; preview consumes existing neutral no_optional_analytics state |
| RootPageHero | Single shared Hero spec and approved seven-page baseline | Existing `hub-dark`, approved RES title/intro/CTA/media slots; no Home/APP open-white override |
| Body articles | Home-family industrial-editorial type, color and CTA system | Research guidance and evidence rather than Home sales modules or Product grade matrix |
| Conditional resource card | Approved B card/action relationship | No thumbnail required; long title and complete Trade metadata can grow naturally |
| FAQ | Approved B full answers | Static expanded articles; no unnecessary collapsed control or answer omission |

Read-only D32 owner comparison found shared `header.php`, `footer.php` and `template-parts/root-page-hero.php`. The latter currently has product-oriented slots; that is not proof of implemented RES hub-dark support. This workset's static Hero projection is explicitly `PROTOTYPE_ONLY`, bound to shared source hashes and owns only RES slots. Gate 6 must request shared-owner integration instead of a RES PHP/CSS fork. Home current Manifest V1.12, Product D32 V0.2 and APP D32 V0.4 are owner/consumption references, not substitutes for RES content authority or D32 evidence.

At 1440 the current scheme uses side-by-side Hero media and three guidance/evidence articles; at 768 and 390 articles stack in source order. Decorative media is absent at ≤900px with zero reserved height. H1 preserves approved content and 56/44/36px type: measured 3/2/4 lines. Header is 84px desktop and 64px narrow. H0 content precedes Footer at all widths without resource-sized blank holes. At 390, Trade metadata labels and values stack and long agency text wraps without clipping. These are structure-verification choices, not a new generic column/spacing lock. Existing page-specific and shared Hero geometry still apply. Gate 4 resolves final body spacing, density, columns and full five-width visual QA.

## Preflight, evidence and limits

Diagnostic self-check: 47/47 planning/static assertions; 92/92 browser checks across H0 1440/768/390, narrow menus, Cookie dialog, H2 1440/390, H4 1440/768/390/720, and JavaScript-disabled FAQ. The full H0 page was manually read in continuous diagnostic frames to Footer at all three core widths; H4 source metadata was inspected at all four widths. No failing content/operation Finding remains before freeze. Formal evidence is produced only after the hash-bound preflight record passes its project checker and the freeze record exists.

Menu actual tests include opening, initial Home focus, current Resources marker, Tab/reverse loop, Escape, selecting Resources with local navigation intercepted only to retain the test document, inert background, overflow restoration and desktop-breakpoint close. Cookie actual tests include entry, opening, close focus, reverse/forward loop, Escape and trigger focus restoration. These tests exercise assembled shared behavior, not inherited claims of a D32 runtime pass. No full-page cookie image is required; runtime checks bind this assembly.

The 720 CSS-px stress case is the reflow equivalent of a 1440-px viewport at 200% zoom, not a native browser zoom measurement. HTTP destinations, child live route eligibility, current official sources, production Schema/cache atomic removal, CMS/editor/migration, WordPress accessibility and native browser zoom remain later Gate 6/8/9 acceptance responsibilities. Gate 4 still owns complete visual/responsive production. No unresolved future dependency is misreported as a Gate 3 runtime defect.

## Submission boundary

The author cannot approve this bundle. Independent reviewer is `/root/about_root_d32`, to be dispatched by controller. Stop at `READY_FOR_INDEPENDENT_GATE3_REVIEW` after formal assets and author self-check are saved. Do not initiate Gate 4, external development, merge, deploy, publish, DNS, indexing or Gate 10. No status/index/shared/history/D32 files are modified.
