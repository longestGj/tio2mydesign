# Gate 4 isolated method validation — evidence entry

This is a synthetic exercise authorized on 2026-09-07. No material here is a real TiO2 product specification or production page. The actual preview uses a neutral Reference Materials Desk shell and locally bundled Inter.

Final exercise outcome: original design A has no required finding in the tested scope; independent review detected all three predeclared controller-injected defects in B; corrected C met all three original acceptance conditions without an observed adjacent regression. This supports limited method use in this sample, not a general success rate or full Agent validation. All three agents finished writing before this snapshot was archived.

## Readable entries

- `input.md`: fixed original task, copy, layout and acceptance conditions.
- `design-v1/index.html`: original method-produced three-region preview; relative dependencies are bundled. Add `?media=absent` to inspect the no-image branch.
- `design-v1/report.md` and `asset-inventory.json`: designer's saved handoff, identity and limitations. Author self-check is not independent review.
- `review-a/`: independent examination of original design-v1.
- `candidate-b/`: controller-injected derivative, intentionally not conforming; never use as a template.
- `review-b/initial-discovery-report.md`: independent initial findings for B.
- `candidate-c/`: controlled correction of B; source functionality and text normalize to original design but HTML/CSS line endings differ, so hashes are separately recorded.
- `review-b/rereview-c/`: re-review of original B findings against C.
- `controller-evidence/`: controller's file, image and local interaction checks.
- `source-snapshots/`: exact Agent, Skill and brand inputs at trial start.
- `controller-plan.md`, `controller-mutations.json`: pre-dispatch design/mutation record, not initial reviewer input.

The trial does not compare a no-Skill arm, and reviewers' lack of access to expected findings does not establish strict experimental blinding. Reviewers and designer used separate agents; re-review of C reuses the B reviewer.

## Permanent archive and paths

Permanent destination: `D:/23MySec/90_archive/skills/2026-09-07/gate4-method-validation/`.
Original run root: `D:/23MySec/99_workspace/gate4-method-validation-2026-09-07/`.

When a historical report uses the original absolute root, substitute the permanent archive root and retain the relative suffix. The copied preview itself uses relative dependencies. Historical scripts can contain absolute runtime or working-directory paths and may write evidence files: do not run them in the frozen archive. Reproduce in a fresh separate directory.

`ARCHIVE_MAP.json` and `ARCHIVE_CHECK.json` in the permanent destination record the final copied file set, sizes and SHA-256 verification. Working originals and all failure/revision records are preserved. The project validation record controls interpretation of final results; these files do not approve an Agent role, a real page, a Gate, development or publication.
