const fs=require('fs');const {PNG}=require('C:/Users/longe/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/pngjs');
const root='D:/23MySec/99_workspace/gate4-method-validation-2026-09-07',name='390-request-list-open-beta-focus.png';const a=PNG.sync.read(fs.readFileSync(root+'/design-v1/exports/'+name)),b=PNG.sync.read(fs.readFileSync(root+'/review-a/'+name));let count=0,minX=a.width,minY=a.height,maxX=0,maxY=0;const pairs={};for(let y=0;y<a.height;y++)for(let x=0;x<a.width;x++){const i=(y*a.width+x)*4;if(a.data.subarray(i,i+4).equals(b.data.subarray(i,i+4)))continue;count++;minX=Math.min(minX,x);maxX=Math.max(maxX,x);minY=Math.min(minY,y);maxY=Math.max(maxY,y);const pair=[...a.data.subarray(i,i+4)]+' -> '+[...b.data.subarray(i,i+4)];pairs[pair]=(pairs[pair]||0)+1}const result={name,sameDimensions:a.width===b.width&&a.height===b.height,count,bounds:{minX,minY,maxX,maxY},frequentPairs:Object.entries(pairs).sort((a,b)=>b[1]-a[1]).slice(0,5)};fs.writeFileSync(root+'/review-a/pixel-difference.json',JSON.stringify(result,null,2));console.log(result);
