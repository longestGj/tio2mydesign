# APP-INK Gate 3 task input card V0.1

`DRAFT_FOR_PROJECT_CONTROL_REVIEW`。Page ID `APP-INK`；English；Application；注册 URL `/applications/titanium-dioxide-for-printing-inks/`；主关键词 `titanium dioxide for printing inks`；`site_scope=tio2-my`。本次授权只覆盖 APP-INK Gate 3 候选执行、执行者自检与 Gate 3→4 handoff draft，不含独立审查、批准、关闭 Gate 3、启动 Gate 4、开发或发布。

当前页面 Manifest V0.3 绑定已批准 A V0.1 / B V0.2 / C V0.2。B 的 `BUYER_COPY_START` 与 `BUYER_COPY_END` 之间是唯一批准可见正文；A 另批准 breadcrumb `Home > Applications > Titanium Dioxide for Printing Inks` 与 eyebrow `PRINTING INKS APPLICATION`。不得改变事实、URL、主关键词、模块顺序、共享合同或批准正文。

固定结构为 11 个模块：Hero → Define the ink and print system → Judge opacity in the printed white layer → Read dispersion, rheology and storage together → Keep grind and particle measurements in context → Use supplier documents to frame the comparison → Compare incumbent and candidate on matched conditions → Compare cost on accepted printed output → Grades to Review → Prepare your request → Technical sources。共享 Header / Footer / Cookie 随后组装。

页面专属风险：四行供应商文件角色关系；四个中立 Grade（M-350、M-510、M-52、M-2377）及固定顺序；三条请求路径和长上下文在 390px 的完整性；六条技术来源及六个外链的完整性；技术长文、文件矩阵与 Grade 表在窄屏转为可读记录时的对象关系。无表单、FAQ、媒体、筛选或选择状态。

三端要求：1440 / 768 / 390，DPR 1，完整页到 Footer；768 / 390 菜单实际打开及选择退出；三端 Cookie Settings 实际开闭与焦点返回；390 两个 Hero 锚点实际操作。共享 Chrome 使用页面中立 Consumer V0.2，`currentPageId=APP-000`，生产 Logo 路径来自批准 Logo Manifest。

输出目录只限 `D:/23MySec/pages/applications/printing-inks/`。正式候选在硬预检 `PASS_FOR_FORMAL_RENDER` 后冻结再渲染；核心证据放 `approval_core`，诊断证据放 `diagnostic_support`。提交后停止于 Project Control 独立审查，不自行批准或关闭 Gate 3。
