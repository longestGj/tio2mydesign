# DOC-TDS Gate 4 Project-Control Submission V0.1

## 1. Decision requested

Approve `DOC-TDS Gate 4 / Direction A — Technical Editorial Decision Desk` and, if desired, separately authorise Gate 5.

Current status: `PROJECT_CONTROL_REVIEW_PASS_PENDING_USER_APPROVAL`.

## 2. What is proposed

- White-led industrial B2B editorial page using Primary Navy `#062B5B`, Deep Navy `#031B3A`, Functional Teal `#008078`, Soft Background `#F5F8FB` and Border `#D9E2EC`.
- Editorial Hero with one primary document-request action and a compact TDS/SDS/COA decision key.
- Deep Navy Direct Answer, restrained document-selection cards, precise Grade context, professional comparison table, checklist, four-step review process, FAQ, related paths and final CTA.
- Exact approved production Logo in Header and reverse Logo in Footer.
- Desktop, Tablet, Mobile, Mobile Menu open and three selection-state evidence.

## 3. Change from Gate 3

Gate 3's grayscale structural evidence is converted into the approved brand system. No module, copy, evidence control, CTA contract, document-selection behaviour or Grade contract is changed. Internal Gate 3 review-strip text is removed from the Buyer Clean visual.

## 4. Pages and functions affected

- Directly affected: `/documents/tds-sds-coa/` only.
- Shared dependency: Global Chrome V0.5 is consumed, not redesigned.
- Related links: `/documents/`, `/documents/reach/`, `/documents/certificate-of-origin/` remain destinations only; their content and Gate status are not changed.
- Receiving form: `/request-documents/` contract remains three document types plus one primary Grade and editable buyer context.

## 5. Validation result

- Automated visual/structural/responsive contract: `PASS`.
- Manual Desktop/Mobile/state review: `PASS`.
- P0/P1/P2: `0/0/0`.
- One Mobile cascade-order regression was found, reproduced with a failing test, fixed in Superdesign v3 and revalidated.

## 6. Remaining risks and limits

- This is Gate 4 visual direction, not the Gate 5 complete visual baseline.
- The Superdesign AI endpoint was unavailable because of account credits; the official import fallback was used, preserving the same project, version history, context and Brand Assets.
- The page must continue to avoid public-download implications and availability promises.
- Existing project-level Evidence Holds remain open and unchanged.
- No development, D16 modification, deployment, publication, DNS or indexing is authorised.

## 7. Project-control recommendation

`APPROVE GATE 4`.

The direction is consistent with the Documents Hub while remaining a distinct decision page. It gives TDS/SDS/COA a clear technical-review identity, passes all required responsive/state checks and introduces no new factual claims. On user approval, Gate 4 can close. Gate 5 should start only if the user explicitly authorises it.

## 8. Expected user decision

The user may respond with one of the following clear decisions:

- `批准 Gate 4，授权启动 Gate 5。`
- `批准 Gate 4，但暂不启动 Gate 5。`
- specific revision instructions.
