const fs=require('fs'),path=require('path'),crypto=require('crypto'),{pathToFileURL}=require('url');const {chromium}=require('C:/Users/longe/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const dir=__dirname,hash=p=>crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex');
(async()=>{const lock=JSON.parse(fs.readFileSync(path.join(dir,'source-lock.json')));for(const x of lock.files)if(hash(x.path)!==x.sha256)throw Error('Source changed '+x.path);const b=await chromium.launch(),p=await b.newPage({deviceScaleFactor:1}),assets=[];fs.mkdirSync(path.join(dir,'approval_core'),{recursive:true});
async function shot(name,options){const file=path.join(dir,'approval_core',name+'.png');await p.mouse.move(0,0);await p.evaluate(()=>new Promise(r=>requestAnimationFrame(()=>requestAnimationFrame(r))));if(options.hero)await p.locator('.rootHero').screenshot({path:file});else await p.screenshot({path:file,fullPage:!!options.full});const bytes=fs.readFileSync(file);assets.push({path:file,sha256:hash(file),bytes:bytes.length,logical_width:p.viewportSize().width,physical_width:bytes.readUInt32BE(16),physical_height:bytes.readUInt32BE(20),dpr:1,state:name,source_sha256:lock.source_identity});}
for(const width of [320,340,341,374,375]){await p.setViewportSize({width,height:1000});await p.goto(pathToFileURL(path.join(dir,'market-visual.html')).href);await p.evaluate(()=>document.fonts.ready);await shot('MARKET-000_D32_G4_R01_HERO_'+width,{hero:true});if(width===320){await p.evaluate(()=>scrollTo(0,0));await shot('MARKET-000_D32_G4_FULL_320',{full:true});await p.locator('#cookie-trigger').click();await shot('MARKET-000_D32_G4_COOKIE_320',{});}}
fs.writeFileSync(path.join(dir,'approval_core/new-assets.json'),JSON.stringify({time:new Date().toISOString(),renderer:await b.version(),source_identity:lock.source_identity,assets},null,2));console.log(JSON.stringify({assets:assets.length}));await b.close();})();
