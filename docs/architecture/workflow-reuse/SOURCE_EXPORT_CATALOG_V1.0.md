# 网站工作流复用：源导出清单 V1.0

2026-09-11。用户批准按五步方案实施。第1步：SOURCE_CATALOG_READY / NOT_INSTALLABLE。清单定义提取来源，不把现有项目文件标为通用已发布包。

## 1. 已确定的范围

- 总控、W1–W5、Gate1、Gate2执行/审查、Gate3、Gate4、独立Gate5、Gate6、Gate9、内链专项：15个现行角色源。
- 搜索意图、响应式线框、品牌应用、完整视觉、布局交互核验、合同一致性、开发交付、运行验收、内链核验：9个现行Skill源。
- 配套参考、预检脚本、工具测试、schema、阶段和交接合同：按机器清单选取，不能只复制Agent/SKILL.md。
- Gate0由总控准入；Gate7不恢复旧职责；Gate8接入现有独立开发工作流；Gate10发布边界保留，不因本次复用自动授权。

## 2. 导出动作

| 类别 | 做法 |
|---|---|
| 通用角色/方法 | 提取判断、边界、输入输出；改写项目链接为包内合同或本站配置 |
| 共用合同/标准 | 合并当前有效条款，去掉历史进度和实例事实；旧版本只作来源追溯 |
| 运行脚本/schema | 进入技术适配，检查路径/环境/阈值依赖，修改后运行针对性测试 |
| 行业模型 | 从现有判断提炼问题与关系结构，不复制TiO2具体产品关系 |
| 本站权限/事实/品牌/页面 | 不导出；初始化空白配置，由本站来源与授权提供 |
| Gate8 | 参考D16现行开发接口；不复制其代码、Git状态或TiO2特殊限制，不新增开发Agent |
| D15 | 借鉴相对路径、文件清单和初始化碰撞处理；不复制其Stage/审批/Markdown-only/单Run规则 |

## 3. 基线差异处理

7份基线核对共58条身份关系，56条直接匹配。Gate4和Gate5两项差异均是后来加入的TiO2一级页面Hero规则；精确移除对应单段后，两份文件均恢复为基线SHA。同步依据在已批准RootPageHero合同，不能将其误当未知修改，也不能把TiO2几何/装饰要求推广到所有新站。

机器清单保留实际SHA、原SHA、差异原文和处理依据。本步骤不回写现有基线或批准源。

## 4. 依赖提取纪律

[机器清单](SOURCE_EXPORT_CATALOG_V1.0.json)逐文件记录源、SHA、类型和拟处理层，并列出直接Markdown依赖分类。旧设计文档可能仍含有效方法条款，不能仅凭文件名为历史就丢弃；第2步按当前批准合同提取有效要求，再改写引用。新母版所有运行必需引用必须闭合；不得靠回到D23读取才能运行。

根入口、Context、Index、Status生成本站模板；原授权记录不复制。共享消费包与Hero规范留在TiO2实例，通用角色只引用本站owner合同。

## 5. 明确不导出的内容

旧Gate4方向Agent、旧Gate5制作Agent、buyer-task-content-design探索草稿不进入默认包。所有页面成果、研究正文、企业资料、批准记录、活动任务、页面Manifest、凭据、Git/个人配置不随控制层复制。历史方法可以在来源库追溯，不作为当前运行入口。

## 6. 下一步及完成标准

第2步按清单建立独立母版；第3步实现配置/初始化；第4步在隔离目录验证；第5步仅对明确授权的目标网站试跑。当前没有声称母版、安装器或跨站能力已完成。第1步静态检查验证文件存在、来源稳定、角色方法数量及基线差异解释，不替代后续完整依赖与行为验证。
