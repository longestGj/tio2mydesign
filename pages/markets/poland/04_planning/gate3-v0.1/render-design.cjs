const fs=require('fs'),path=require('path'),crypto=require('crypto'),{pathToFileURL}=require('url');
const{chromium}=require('C:/Users/longe/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const root=__dirname,file=path.join(root,'MARKET-EU-PL_GATE3_RESPONSIVE_WIREFRAME_V0.1.html');
(async()=>{const b=await chromium.launch({executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe',headless:true,args:['--disable-background-networking']});const p=await b.newPage({viewport:{width:1440,height:1000},deviceScaleFactor:1});const info={source:file,sourceSha256:crypto.createHash('sha256').update(fs.readFileSync(file)).digest('hex'),renderer:await b.version(),scale:1,exports:[],blockedRequests:[],errors:[]};await p.route(/^https?:/,r=>{info.blockedRequests.push(r.request().url());r.abort()});
async function shot(name,state,fullPage=true){const target=path.join(root,name+'.png');await p.screenshot({path:target,fullPage});const buffer=fs.readFileSync(target);info.exports.push({path:target,state,logicalViewport:p.viewportSize(),physical:{width:buffer.readUInt32BE(16),height:buffer.readUInt32BE(20)},scale:1,bytes:buffer.length,sha256:crypto.createHash('sha256').update(buffer).digest('hex')});}
try{for(const width of[1440,768,390]){await p.setViewportSize({width,height:1000});await p.goto(pathToFileURL(file).href);await p.evaluate(async()=>{await document.fonts.ready;await Promise.all([...document.images].map(i=>i.decode()))});await shot('MARKET-EU-PL_G3_FULL_'+width+'_V0.1','initial, full page');if(width!==1440){await p.locator('.menuButton').click();await shot('MARKET-EU-PL_G3_MENU_'+width+'_V0.1','shared mobile menu open',false);await p.keyboard.press('Escape');}await p.locator('#cookie-trigger').click();await shot('MARKET-EU-PL_G3_COOKIE_'+width+'_V0.1','shared no_optional_analytics Cookie settings open',false);await p.keyboard.press('Escape');}}catch(e){info.errors.push(e.stack);process.exitCode=1}finally{fs.writeFileSync(path.join(root,'render-evidence.json'),JSON.stringify(info,null,2));await b.close();console.log(JSON.stringify(info,null,2))}})();
